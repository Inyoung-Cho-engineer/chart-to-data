# chart-to-data 서비스 기획서 (PRD)

- 작성자: [이름]
- 작성일: (오늘 날짜)

---

## 1. 추진 배경 (문제 정의)
*누가, 어떤 업무에서, 어떤 불편을 겪는가*

설계 검증이 필요한 연구실/팀원, 그리고 공학 분야 연구자·개발자는 논문·기술 보고서 등에 실린 라인차트에서 수치를 확보해야 하는 경우가 많습니다. 그러나 이런 자료는 대부분 PDF나 이미지(JPG) 형태라 원본 수치 데이터가 함께 제공되지 않습니다.

그 결과 그래프 위의 좌표값을 눈으로 읽고 손으로 옮겨 적는 수작업이 반복되며, 이 과정에서 시간이 많이 들고 사람에 따라 읽는 값이 달라져 오차가 생깁니다. 옮겨 적은 값을 다시 설계 기준값과 하나씩 대조하는 작업도 부담이 됩니다.

이 앱은 PDF·JPG 자료의 라인차트를 자동으로 인식해 그래프 위 개별 좌표값(데이터 포인트)을 추출하는 도구입니다. (추출값을 기준값과 비교·검토하는 기능은 이번 버전 범위에서 제외하고 다음 단계로 보류합니다 — 6장 참고)

## 2. 현행 방식과 한계
*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운가*

**현행 방식**
- 논문·보고서의 그래프를 화면에 띄우거나 출력한 뒤, 눈으로 좌표를 읽어 엑셀 등에 직접 입력
- 필요할 경우 이미지를 확대해 눈금과 데이터 포인트의 위치를 어림잡아 판독
- 옮겨 적은 값을 설계 기준값과 하나씩 수동으로 대조

**한계**
- 판독에 시간이 오래 걸리고, 그래프 개수가 늘어날수록 부담이 비례해서 커짐
- 읽는 사람에 따라 값이 달라져 재현성이 낮음
- 어느 값이 어디서 나왔는지 기록이 남지 않아 검증 근거를 되짚기 어려움
- 기준값 대조가 수작업이라 기준을 벗어난 지점을 놓치기 쉬움

## 3. 목표와 기대효과 (성공 기준)
*측정 가능한 숫자로 (처리 시간, 문의 건수, 정답률 등)*

| 지표 | 현재 | 목표 |
|---|---|---|
| 그래프 1개당 좌표 추출 시간 | 10~20분 (수작업) | 3분 이내 |
| 추출 좌표의 오차율 | 판독자마다 편차 발생 | 축 범위 대비 ±3% 이내 |
| 동일 그래프 재추출 시 값 일치율 | 사람마다 다름 | 95% 이상 |
| 추출 결과 검토 소요 시간 | 별도 대조 작업 필요 | 화면에서 즉시 확인 |

**측정 방법**
- 정답 좌표값을 미리 알고 있는 **검증용 그래프 5~10개를 별도로 준비**한다 (직접 만든 그래프, 또는 원본 데이터를 알고 있는 그래프)
- 오차율은 검증용 그래프의 정답값과 추출값의 차이로, 재추출 일치율은 같은 검증용 그래프를 여러 번 추출해 값이 얼마나 같게 나오는지로 계산한다

**기대효과**
- 수작업 판독·전사 시간을 크게 줄여 검증 본연의 판단에 집중할 수 있음
- 동일한 그래프에서 항상 같은 값이 나와 검증 결과의 재현성 확보
- 추출값과 신뢰도가 CSV로 함께 기록되어 나중에 근거를 되짚어볼 수 있음

## 4. 사용자와 이용 흐름
*누가 쓰는지와 사용 순서 (화살표로)*

**사용자**
- 설계 검증이 필요한 연구실 구성원/팀원
- 공학 분야 연구자·개발자

**이용 흐름**

PDF/JPG 파일 업로드 → 화면에 뜬 이미지에서 사용자가 라인차트 영역을 드래그로 지정 → 축 유형(선형/로그)·범위·단위 확인 및 보정 → 그래프에 선이 여러 개면 추출할 계열 1개 선택 → 그래프 위 좌표값 자동 추출 → 추출 결과를 원본 이미지와 나란히 확인 (신뢰도 낮은 구간은 "확인 필요" 표시) → CSV로 결과 내려받기

## 5. 주요 기능 (must / nice로 구분)

### [must] 1) 그래프 좌표 추출 기능
- 설명: 업로드한 PDF/JPG 안에서 라인차트를 인식하고, 그래프 위 개별 좌표값(데이터 포인트)을 수치 데이터로 추출

- **추출 방식**: 지정한 영역을 서버 쪽 코드에서 외부 AI 비전 모델에 보내 **그래프 영역·축 눈금·계열 색상·교차 구간**을 알아내고, **선의 좌표를 따라가는 정밀 측정은 앱(브라우저)이 그 색을 기준으로 픽셀을 훑어 직접 수행**한다. 이미지가 브라우저에서 외부로 직접 나가지 않도록, 모델 호출은 반드시 서버 쪽에서만 수행한다.
  - (2026-08-13 변경) 처음에는 좌표까지 AI에게 읽게 했으나, 검증용 그래프 실측에서 오차 평균 5.9~7.7%(최대 19.8%)로 아래 목표(±3%)를 못 맞추고 실행마다 값이 달라졌다. 앱이 색으로 픽셀을 추적하면 같은 조건에서 오차 0.1%·재현성 100%가 나와 방식을 바꿨다. 새 서버나 차트 전용 모델을 쓰는 것은 아니다.

- **차트 영역 지정**: 앱이 그래프 위치를 자동으로 찾지 않는다. 화면에 띄운 이미지 위에서 **사용자가 드래그로 라인차트 영역을 직접 지정**하고, 그 영역만 잘라내어 판독에 사용한다. 한 페이지에 그래프가 여러 개면 원하는 것을 골라 지정하면 된다.

- **입력 파일 제한**: 업로드 파일은 **20MB 이하, PDF는 30페이지 이하**로 제한한다. 이를 넘으면 판독을 시도하지 않고, 필요한 페이지만 따로 잘라 올리도록 안내 문구를 보여준다.

- **실패 안내**: 실패는 아래 세 단계로 구분해 각각 다른 안내 문구를 보여준다. 오류 메시지를 그대로 노출하지 않고 사용자가 무엇을 해야 할지 알 수 있는 말로 바꿔 보여준다.
  1. 파일 단계 — 형식이 안 맞거나(PDF/JPG 아님) 용량·페이지 제한 초과
  2. 영역 단계 — 지정한 영역에서 라인차트를 찾지 못함
  3. 모델 응답 단계 — 모델 호출 실패, 응답 시간 초과, 응답 형식이 예상과 다름

- **여러 계열(선) 처리**: 한 그래프 안에 선이 여러 개인 경우, 모든 선을 한 번에 추출하지 않는다. 인식된 계열 목록을 사용자에게 보여주고 **한 번에 1개 계열만 선택해 추출**한다. 다른 계열이 필요하면 선택을 바꿔 다시 추출한다.

- **좌표를 뽑는 규칙**: 선택한 계열에 대해 **그래프 가로축을 화면상 균등하게 나눈 50개 지점**(로그 축이면 값 간격은 로그 균등이 됨)의 값을 뽑아 곡선 전체 모양을 보여준다. 여기에 더해, 사용자가 **검증에 필요한 특정 X값을 직접 입력하면 그 지점의 값을 추가로 질의**할 수 있게 한다.

- AI가 꼭 지켜야 할 규칙:
  - 추출된 좌표값은 원본 그래프의 축 범위·단위를 벗어나면 안 됨 (예: X축이 0~100이면 추출값도 그 범위 내)
  - **추출 전에 각 축이 선형인지 로그 스케일인지 판별해 사용자에게 표시하고, 사용자가 확인·수정한 뒤에만 추출을 진행한다. 축 유형을 자동으로 판별하지 못한 경우에는 임의로 선형으로 가정하지 말고 사용자가 직접 지정하게 한다.**
  - **자동 인식된 축 범위·단위·축 유형은 추출 전 화면에 반드시 노출하고, 사용자가 직접 수정할 수 있게 한다.** (자동 인식값을 사용자 확인 없이 그대로 쓰지 않는다)
  - 추출 결과는 반드시 표/리스트 형태로 사용자에게 보여주고, 원본 그래프 이미지와 나란히 비교할 수 있게 함
  - 좌표값을 요청할 때 **각 지점의 신뢰도(high/medium/low)도 함께 요청**하고, 모델이 low로 응답한 지점은 "확인 필요"로 표시
  - 계열이 서로 교차하는 구간은 값이 뒤바뀔 수 있으므로 "확인 필요"로 표시
  - 서버에 결과를 저장하지 않으므로, **추출값·신뢰도를 CSV로 내려받는 기능을 반드시 제공**해 사용자가 결과를 보관할 수 있게 함

### [nice] 부가 기능
- 추출 결과를 원본 이미지 위에 겹쳐 보며 확인

## 6. 범위와 비범위
*이번 실습에서 만들 것 / 만들지 않을 것*

**범위 (이번에 만들 것)**
- 그래프 좌표 추출 기능 (PDF/JPG 업로드 → 라인차트 인식 → 축 유형·범위 확인 및 보정 → 계열 1개 선택 → 개별 좌표값 추출 → 표/리스트 표시, 낮은 신뢰도 구간 "확인 필요" 표시 → CSV 내려받기)

**비범위 (이번에 만들지 않을 것)**
- 기준값 비교·검토 기능 (구간별 상한·하한 입력, 이탈 지점 강조, 추출값·기준값·차이 비교) — **다음 단계로 보류.** 이번 버전은 좌표를 정확히 뽑아 CSV로 내려받는 것까지가 범위
- 설계 문서·요구사항 지식베이스와의 자동 연결/검증 (다음 단계 목표로 보류)
- 라인차트 외 다른 그래프 유형(막대, 산점도, 파이차트 등) 인식
- 한 그래프의 여러 계열을 한 번에 일괄 추출 (이번에는 1개씩만 선택 추출)
- 차트 추출 전용 모델(LineFormer 등)을 별도 파이썬 서버로 띄워 사용 — 검토했으나 Next.js 한 프로젝트로 처리한다는 원칙에 맞지 않아 이번 범위에서 제외
- 여러 사용자가 함께 쓰는 협업/공유 기능 (계정, 권한 관리 등)
- 다국어 지원
- 모바일 앱
- 대량 파일 일괄 처리

## 7. 보안·개인정보 검토
*사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법*

**사용 문서의 공개등급**
- 좌표 추출을 위해 업로드한 이미지는 **외부 AI 비전 모델로 전송되어 판독**된다. 즉, 파일이 우리 서버 밖(모델 제공사)까지 나간다.
- 이 때문에 이번 실습에서는 **공개된 논문·기술 보고서 등 외부 공개 자료만 사용**한다.
- 미공개 사내 설계 문서나 대외비 자료는 외부로 전송되면 안 되므로 이번 범위에서 제외한다.

**개인정보 여부**
- 처리 대상은 그래프 이미지와 수치 데이터뿐이며, 개인정보는 다루지 않음
- 회원가입·로그인 기능이 없어 이용자 개인정보를 수집·저장하지 않음
- 업로드된 파일과 추출 결과는 **서버(DB)에 저장하지 않는다.** 작업 내용은 브라우저 화면에만 남아 있으며, 새로고침하거나 창을 닫으면 사라진다
- 결과를 남기고 싶으면 사용자가 **CSV로 직접 내려받아** 보관한다 (5번 기능 참고)

**API 키 관리 방법**
- 모든 키는 `.env` 파일에 보관하고, 코드에 직접 적어 넣지 않음
- `.env`는 `.gitignore`에 등록해 저장소(GitHub)에 올라가지 않도록 함
- 키를 쓰는 호출은 서버 쪽 코드에서만 처리하고, 브라우저로 노출되지 않게 함
- 배포 시에는 배포 환경의 환경변수 설정에 키를 등록해 사용

## 8. 기술 스택
*개발에 사용할 도구 모음 (이 과정에서는 Next.js로 고정)*

- **Next.js** — 화면과 서버 쪽 코드(API)를 한 프로젝트에서 처리

**고정 이유**
- Part 1~2 실습에서 쓴 것과 같은 구성이고, Part 5에서 Vercel로 배포할 예정이라 그대로 이어지게 하기 위함

---

## 9. 개발 단위
*핵심 기능을 만들기 좋은 작은 작업 단위로 나눈 목록 (만들 순서대로)*

### 준비
1. Next.js 프로젝트 기본 화면 만들기 (빈 페이지 하나가 뜨는 것까지)
2. `.env`의 API 키를 서버 쪽에서 읽어오는 설정 넣기

### 기능 1 — 그래프 좌표 추출
3. PDF/JPG 파일을 고르는 업로드 화면 만들기 (20MB·30페이지 초과 시 안내 문구)
4. 업로드한 파일을 화면에 이미지로 띄우기 (PDF는 이미지로 변환해서 표시)
5. 이미지 위에서 드래그로 영역을 지정하고 그 부분만 잘라내는 기능 만들기
6. 잘라낸 이미지를 서버로 보내는 API 경로 만들기 (아직 판독 없이 잘 받았는지만 확인)
7. 서버에서 외부 AI 비전 모델을 호출해 응답을 받아오기 (결과를 그대로 화면에 출력)
8. 모델 응답에서 축 유형(선형/로그)·축 범위·단위를 뽑아내기
9. 축 정보를 화면에 보여주고 사용자가 직접 고칠 수 있는 입력칸 만들기
10. 축 유형을 판별하지 못했을 때 사용자에게 직접 고르게 하는 처리 넣기
11. 그래프 안의 계열(선) 목록을 뽑아 화면에 보여주기
12. 계열 1개를 선택하는 기능 만들기
13. 선택한 계열에서 X축 균등 50개 지점의 좌표값과 지점별 신뢰도(high/medium/low)를 함께 요청해 받아오기 (구현 방식은 DESIGN.md §2 B안·§5.3 참조 — AI에게는 위치만 요청하고 실제 값은 앱이 계산)
14. 추출된 좌표값을 표로 보여주기
15. 사용자가 특정 X값을 입력해 그 지점의 값을 추가로 질의하는 기능 만들기
16. 원본 이미지와 추출 결과 표를 나란히 배치하기
17. 축 범위를 벗어난 값이 있으면 걸러내는 검사 넣기
18. 신뢰도가 낮은 구간과 계열이 교차하는 구간을 "확인 필요"로 표시하기
19. 추출값과 신뢰도를 CSV로 내려받는 기능 만들기

### 마무리
20. 파일 단계 실패(형식·용량·페이지 초과) 안내 문구 넣기
21. 영역 단계 실패(라인차트 못 찾음) 안내 문구 넣기
22. 모델 응답 단계 실패(호출 실패·시간 초과·형식 오류) 안내 문구 넣기
23. 정답값을 아는 검증용 그래프 5~10개 준비하기
24. 검증용 그래프로 오차율·재추출 일치율을 재보고 목표(±3%, 95%) 대비 확인하기
25. 전체 흐름을 처음부터 끝까지 한 번 돌려보며 다듬기
