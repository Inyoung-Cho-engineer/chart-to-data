# VERIFICATION.md — 검증 결과 (PLAN.md 28~30번)

> 최근 측정일: **2026-08-14** / 이전 측정: 2026-08-13
> 검증용 그래프: [public/test-charts/](public/test-charts/README.md) — C1~C8(합성 8개) + **R1~R4(논문형 4개, 2026-08-14 추가)**
> 자동 측정 화면: `npm run dev` 후 http://localhost:3000/dev-verify (개발 모드 전용)

---

## 요약 (2026-08-14 기준)

| 기준 | 목표 | 결과 | 판정 |
|---|---|---|---|
| **S1** 전체 흐름 완주 | 끊김 없이 | 업로드→영역→축→계열→추출→CSV 통과 (실제 논문 PDF로 재확인) | ✅ |
| **S2** 추출 오차 | 축 범위 대비 ±3% | **12개 그래프 18개 계열 중 17개 통과** (최대 오차 0.82%). 실패 1개 = 흑백 점선 | ⚠️ 조건부 |
| **S3** 재추출 일치율 | 95% 이상 | 같은 입력 반복 → 완전 동일 (100%) | ✅ |
| **S4** 그래프 1개당 소요 | 3분 이내 | 7초 수준 | ✅ |
| **S5** 이미지가 브라우저에서 외부로 직접 안 나감 | 외부 호출 0건 | 판독용 조각만 `/api/analyze`로. 추적용 원본 조각은 브라우저 밖으로 안 나감 | ✅ |
| **S6** 실패 3단계 안내 | 각기 다른 문구 | 파일·영역·모델 3단계 확인 | ✅ |
| **S7** 새로고침 시 소멸 | 상태 초기화 | localStorage·sessionStorage 0개 | ✅ |

---

## 2026-08-14 재측정 — 실제 논문 PDF에서 드러난 문제와 수정

사용자가 실제 논문형 PDF(`hard_chart_testset_mixed_layout.pdf`, 4쪽 아래 그래프, Condition-A)로 돌린 결과
**50개 중 30개가 "확인 필요"였고, 나머지 값도 전부 틀렸습니다.** 원인을 셋으로 나눠 실측했습니다.

### 원인 1 — PDF를 너무 작게 그려서 선 색이 사라졌다

| 렌더 배율 | 그래프 안에 남은 계열 색(#1f77b4 근처) 픽셀 |
|---|---|
| 1.5 (기존) | **6개** |
| 4 (긴 변 3000px 기준) | **1369개** |

배율 1.5에서는 얇은 선이 안티에일리어싱으로 흰색과 섞여, 이미지 어디에도 계열 색이 남지 않았습니다.
→ **[pdf.ts](src/lib/pdf.ts)**: 페이지 긴 변이 3000px이 되도록 배율을 정하게 바꿈.
→ **[crop.ts](src/lib/crop.ts)**: 추적용 조각은 줄이지도 JPEG로 압축하지도 않는 별도 이미지(PNG)로 분리.

### 원인 2 — 연회색 축 선이 "계열 색의 흐린 부분"으로 묶였다

계열 색이 `(64,124,160)`일 때 축 선 회색 `(134,134,134)`의 거리가 106으로 `MERGE_DISTANCE`(110) 안쪽에 들어와,
축 선이 경쟁 후보에서 빠졌습니다. 그 결과 **50지점 전부가 축 선을 따라가 Y축 최솟값(1)** 으로 나왔습니다.
→ **[trace.ts](src/lib/trace.ts)**: 계열 색이 뚜렷한 유채색이면 무채색은 거리와 무관하게 항상 경쟁 후보로 남김.

### 원인 3 — 테두리를 축 최솟값~최댓값으로 착각했다 (가장 큰 오차)

논문 그래프는 데이터 양옆에 여백이 있어 **테두리 ≠ 눈금 범위**입니다.
이번 샘플은 눈금이 1~7인데 테두리는 실제로 **0.27~7.73**에 해당했습니다.

| 위치 | 예전 계산 | 실제 값 | 오차 |
|---|---|---|---|
| 테두리 위 | 7 | 7.73 | 9.4% |
| 테두리 아래 | 1 | 0.27 | **57%** |

→ **[plotBox.ts](src/lib/plotBox.ts)**: 축 선 바깥의 눈금 표시(tick)를 찾아
"최솟값 눈금·최댓값 눈금이 어디인지"를 따로 재는 `AxisCalibration` 추가. 못 찾으면 예전처럼 테두리를 쓴다.

**수정 후 같은 PDF 재측정: 50/50 추출, 확인 필요 0개.** 추출점을 원본 위에 겹쳐 그려 곡선에 정확히 얹히는 것을 눈으로 확인.

---

## S2 상세 — 검증용 그래프 자동 측정 (2026-08-14)

오차 = `|추출값 − 정답값| / (yMax − yMin) × 100`
측정 화면: http://localhost:3000/dev-verify (외부 모델을 부르지 않고 앱 계산 부분만 잰다)

| # | 그래프 | 확인 항목 | 찾음 | 평균 오차 | 최대 오차 | 판정 |
|---|---|---|---|---|---|---|
| C1 | Basic Single Line | 기본 형태 | 50/50 | 0.06% | 0.26% | ✅ |
| C2 | Log Y Axis | 로그 축 | 50/50 | 0.01% | 0.16% | ✅ |
| C3 | Solid + Dashed | 점선·교차·범례 안쪽 | 50/50 ×2 | 0.11% / 0.24% | 0.82% | ✅ |
| C4 | Three Series | 계열 3개 | 50/50 ×3 | 0.07 / 0.05 / 0.05% | 0.62% | ✅ |
| C5 | Grayscale Dashed | **흑백 점선** | 50/50 · 42/50 | 0.05% / **22.97%** | 49.87% | ❌ (점선) |
| C6 | Line with Markers | 데이터 마커 | 50/50 | 0.08% | 0.34% | ✅ |
| C7 | Gridlines + Frame | 격자선·테두리 | 50/50 | 0.06% | 0.38% | ✅ |
| C8 | Steep near Axis | 축에 붙는 급한 곡선 | 50/50 | 0.07% | 0.38% | ✅ **(이전 27.5% → 해결)** |
| R1 | Margins + Legend Inside | **논문형: 여백·격자·안쪽 범례** | 50/50 ×2 | 0.07 / 0.05% | 0.23% | ✅ |
| R2 | Margins + Log Y | 논문형: 여백 + 로그 축 | 50/50 | 0.03% | 0.50% | ✅ |
| R3 | Curve Above Top Tick | 논문형: 곡선이 최댓값 눈금 위로 | 50/50 | 0.09% | 0.59% | ✅ |
| R4 | Thin Lines ×3 | 논문형: 얇은 선 3개 | 50/50 ×3 | 0.07% ×3 | 0.35% | ✅ |

**통과: 11개 그래프 / 17개 계열** — 전부 최대 오차 0.82% 이하로 목표(±3%)를 크게 밑돕니다.

### 새 그래프(R1~R4)가 실제로 이번 버그를 잡아내는가

`?noticks=1`로 눈금 보정을 끄면(= 예전 방식) 결과가 이렇게 나빠집니다.

| 그래프 | 눈금 보정 켬 | 눈금 보정 끔(예전 방식) |
|---|---|---|
| R1 Condition-A | 50/50 · 0.07% | 43/50 · **4.07%** (최대 7.97%) |
| R1 Condition-B | 50/50 · 0.05% | 43/50 · **6.10%** |
| R3 Curve | 50/50 · 0.09% | 46/50 · **4.40%** |
| R4 Type A | 50/50 · 0.07% | 46/50 · **2.34%** |
| C1~C8 | 변화 거의 없음 | 변화 거의 없음 (테두리 = 눈금 범위라서) |

C1~C8만으로는 이 문제를 절대 잡을 수 없었다는 뜻입니다.

---

## 남은 한계

### C5 흑백 점선 (평균 23%) — 미해결, 구조적 한계

이 앱의 추출은 **계열 색으로 선을 구분**합니다(DESIGN §5.5). 흑백 차트는 실선(검정)·점선(회색)·축선(진회색)이
모두 무채색이라 색으로 갈라낼 수 없습니다. 임계값 조정으로는 해결되지 않습니다.

**대응**: 흑백 차트는 지원 대상이 아닙니다. 원본이 컬러라면 컬러본을 쓰세요.

### 눈금을 못 찾는 그래프

축 선 바깥에 눈금 표시가 없는 그래프(눈금을 안쪽으로 그리거나 아예 없는 스타일)는 눈금 보정이 안 되고
예전처럼 테두리를 축 범위로 씁니다. 이때 여백이 있는 그래프라면 위에서 본 만큼 값이 틀어집니다.
`/dev-verify`의 "눈금보정 X/Y" 칸이 X면 이 경우입니다. 결과를 눈으로 확인하세요.

### AI 판독 단계의 실행 간 편차

픽셀 추적 자체는 같은 입력이면 **항상 같은 값**을 냅니다(S3 100%). 그런데 그 앞단의 AI 판독(`/api/analyze`)은
호출할 때마다 조금씩 다르게 답합니다.

| 항목 | 관찰된 편차 |
|---|---|
| 축 최솟값 | 같은 그래프를 한 번은 `0~7`, 다른 번은 `1~7`로 답함 → **사용자가 축 확인 화면에서 반드시 확인할 것** |
| 계열 색상 | 실제 `#1f77b4`인 선을 `#00aaff`로 답한 적 있음 → 색 팔레트 대조로 보완(DESIGN §5.5 A) |
| plotBox | `{0.1, 0.1, 0.9, 0.9}` 식 반올림 → 축 선 픽셀 감지로 보완(DESIGN §5.6) |

**뜻하는 것**: 같은 세션에서 다시 추출하면 값이 같지만, **`분석`을 다시 누르면 결과가 조금 달라질 수 있습니다.**
검증 근거로 쓸 값은 CSV로 받아 보관하세요.

---

## 다시 측정하는 방법

```bash
npm run dev
```

- 자동 측정: http://localhost:3000/dev-verify — 12개 그래프를 한 번에 재고 표로 보여줍니다.
- 예전 방식과 비교: http://localhost:3000/dev-verify?noticks=1
- 눈으로 보기: http://localhost:3000/test-charts/ — 그래프를 그려 보고 JPG로 내려받을 수 있습니다.

`public/test-charts/charts.js`의 수식을 고치면 정답도 바뀌므로, 고칠 때는 README의 표도 함께 고쳐주세요.
