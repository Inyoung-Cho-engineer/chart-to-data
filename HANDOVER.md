# HANDOVER.md — 인수인계

> 작성일: 2026-08-13 / 다음 세션이 바로 이어서 작업할 수 있도록 정리한 문서
> **먼저 [CLAUDE.md](CLAUDE.md)의 작업 규칙을 읽으세요.** 이 문서는 "지금 어디까지 왔고 무엇을 조심해야 하는가"만 다룹니다.

---

## 1. 한 줄 요약

논문·보고서의 라인차트에서 좌표 50개를 뽑아 CSV로 내려받는 Next.js 앱.
**기능은 전부 동작하고 검증까지 마쳤습니다. 남은 것은 Vercel 배포(PLAN 31번) 하나입니다.**

---

## 2. 지금 상태

### 진행률: PLAN.md 31단계 중 **30개 완료**

| 단계 | 내용 | 상태 |
|---|---|---|
| 1~5 | 뼈대·env·타입·상태저장소·오류문구 | ✅ |
| 6~8 | 업로드·파일검사·이미지 표시(PDF 변환 포함) | ✅ |
| 9~11 | 드래그 영역 지정·잘라내기·서버 API | ✅ |
| 12~17 | AI 판독·응답 검증·축 확인 UI | ✅ |
| 18~26 | 계열 선택·좌표 추출·결과 표·추가 질의·CSV | ✅ |
| 27~30 | 실패 문구 점검·검증용 그래프·오차 측정·전체 흐름 | ✅ |
| **31** | **Vercel 배포 + 환경변수 등록** | ⬜ **남음** |

### 검증 결과 (자세히는 [VERIFICATION.md](VERIFICATION.md))

- 검증용 그래프 8개 중 **6개(9개 계열)가 평균 오차 0.3% 이하**로 목표 ±3% 통과
- 재현성 **100%** (같은 입력 3회 반복 → 50/50 완전 동일)
- 전체 흐름 **7초** 완주, 외부 도메인 직접 호출 0건, 새로고침 시 완전 초기화

---

## 3. 바로 다음에 할 일

### 우선: 실제 논문으로 검증 (사용자가 진행 중)

사용자가 실제 논문 스캔본으로 시험해보는 중입니다. **지금까지 검증은 전부 합성 그래프였습니다.**
사용자가 문제를 보고하면 그것부터 처리하세요. 볼 지점:

- 축 값이 맞게 읽히는지 (틀리면 뒤 값이 전부 밀림)
- 흑백 그래프 (구조적 한계 — 아래 5장)
- 곡선이 X축에 바짝 붙는 구간 (미해결 — 아래 5장)
- "확인 필요" 표시가 적절한지

### 그 다음: PLAN 31번 — Vercel 배포

- `.env`의 `VERCEL_TOKEN`으로 Vercel CLI 인증 (**토큰 값을 채팅·로그에 출력하지 말 것** — CLAUDE.md 규칙)
- Vercel 환경변수에 `OPENAI_API_KEY` 등록 (`NEXT_PUBLIC_` 접두사 절대 금지)
- 배포 후 확인할 것: `/api/analyze`가 60초 제한 안에 도는지, 요청 본문 4.5MB 제한에 안 걸리는지
  (DESIGN §8.3에 대비책 있음 — 조각을 1600px·JPEG로 줄여 보내므로 보통 200~500KB)

---

## 4. 이 앱이 동작하는 방식 (핵심만)

### 하이브리드 판독 — 가장 중요한 설계 결정

```
AI (외부 모델, /api/analyze 1회만 호출)
  → 그래프 영역(plotBox), 축 눈금·범위·단위, 계열 목록과 색상, 교차 구간

앱 (브라우저, 서버·AI 안 씀)
  → plotBox를 축 선 픽셀로 보정  (plotBox.ts)
  → 계열 색을 따라 픽셀 추적      (trace.ts)
  → 축 변환식으로 실제 값 계산    (axis.ts)
```

**왜 이렇게 했나**: 처음에는 AI에게 좌표까지 물었는데 실측 오차가 평균 5.9~7.7%(최대 19.8%)였고
같은 그래프도 실행마다 값이 달랐습니다. 앱이 픽셀을 추적하니 0.1%·재현성 100%가 나왔습니다.
자세한 경위는 DESIGN.md §2(설계안 D안)에 있습니다.

> ⚠️ **이 방식을 되돌리지 마세요.** AI에게 좌표를 물어보는 방식은 이미 측정으로 기각됐습니다.
> `/api/extract`·`/api/point`는 제거되어 `trash-can/hybrid-transition-2026-08-13/`에 있습니다.

### 화면 흐름

`page.tsx` 한 페이지. 왼쪽 `ImageViewer`(원본 계속 표시) + 오른쪽 패널들이 순서대로 나타남:
`UploadPanel` → `RegionPanel` → `AxisPanel` → `SeriesPanel` → `ExtractPanel`, 아래 `ResultTable`.

각 패널은 앞 단계가 끝나야 렌더링됩니다(예: 축을 확인해야 `SeriesPanel`이 나타남).

### 파일 지도

| 파일 | 역할 | DESIGN 절 |
|---|---|---|
| `src/lib/trace.ts` | **계열 색 픽셀 추적** — 이 앱 정확도의 핵심 | §5.5 |
| `src/lib/plotBox.ts` | 축 선을 찾아 AI의 plotBox 보정 | §5.6 |
| `src/lib/axis.ts` | 위치↔값 변환식(선형/로그), DataPoint 생성 | §5.3 |
| `src/lib/vision.ts` | **서버 전용** — OpenAI 호출. API 키는 여기서만 | §6.1 |
| `src/app/api/analyze/route.ts` | 외부 모델을 부르는 **유일한** 경로 | §6.1 |
| `src/lib/schema.ts` | Zod로 모델 응답 형식 고정 | §6.1 |
| `src/lib/errors.ts` | 실패 3단계 문구 표 (원본 에러 노출 금지) | §7 |
| `src/lib/pdf.ts` / `crop.ts` / `csv.ts` | PDF→그림 / 영역 자르기 / CSV 생성 | §8.2 |
| `src/store/session.ts` | 브라우저 세션 상태(Zustand). 서버 저장 없음 | §5.2 |
| `public/test-charts/` | 검증용 그래프 8개 + 정답 수식 | §11 |

### 의존성 (전부 목적이 있음)

`next` `react` — 뼈대 / `zustand` — 세션 상태 / `pdfjs-dist` — PDF를 브라우저에서 그림으로
`openai` — 비전 모델 호출 / `zod` — 모델 응답 형식 검사 / `tailwindcss` — 화면

---

## 5. 알려진 한계 (실측으로 확인, 미해결)

| 상황 | 결과 | 성격 |
|---|---|---|
| **흑백 점선 차트** | 오차 10~24% | **구조적 한계.** 색으로 선을 구분하는 방식이라 무채색은 갈라낼 수 없음. 임계값 조정으로 해결 불가 |
| **곡선이 축 선에 바짝 붙음** | 오차 27% | 축 선을 데이터로 오인. plotBox 가장자리 제외를 시도했으나 다른 그래프를 악화시켜 되돌림 |
| **AI 판독의 실행 간 편차** | `분석`을 다시 누르면 결과가 달라질 수 있음 | 픽셀 추적은 100% 재현되나, AI가 주는 색상·plotBox가 매번 조금씩 다름 |

---

## 6. 함정 — 이것 모르고 건드리면 고생합니다

### ⚠️ `trace.ts`의 임계값은 서로 얽혀 있습니다

`MAX_COLOR_DISTANCE`, `MERGE_DISTANCE`, `MAX_JUMP_RATIO`, 넓히기 상한 — 하나를 바꾸면
다른 그래프가 깨집니다. 실제로 겪은 일:

- 팔레트를 통째로 경쟁시킴 → C3 고쳐졌지만 C6이 50/50 → 27/50으로 나빠짐
- 비슷한 색을 묶음 → C6·C7 고쳐졌지만 C1·C4가 깨짐
- plotBox 가장자리 3px 제외 → C8 못 고치고 C7이 0.13% → 12.05%로 나빠짐

> **`trace.ts`나 `plotBox.ts`를 고치면 반드시 검증용 그래프 8개 전부를 다시 측정하세요.**
> 하나만 보고 "고쳤다"고 판단하면 안 됩니다.

### ⚠️ 브라우저 자동 측정 시 주의

검증을 자동화할 때 겪은 두 가지:

1. **`setTimeout` 기반 대기 금지** — 브라우저 탭이 숨겨져 있으면 타이머가 분당 1회까지 느려집니다.
   `MutationObserver`로 DOM 변화를 감지해 기다리세요.
2. **마우스 이벤트는 틱을 줘야 함** — `mousedown` 직후 바로 `mouseup`을 쏘면 React가 아직
   window 리스너를 안 붙여서 무시됩니다. 사이에 `await new Promise(r=>setTimeout(r,0))`.

측정 스크립트는 매번 새로 짰습니다(파일로 남기지 않음). 필요하면 VERIFICATION.md의 측정 방법을 참고하세요.

### ⚠️ 지키고 있는 규칙들 (깨지 마세요)

- **API 키는 `src/lib/vision.ts`에서만** 사용. `NEXT_PUBLIC_` 접두사 금지
- **원본 에러 메시지를 화면에 노출 금지** — `errors.ts`의 정해진 문구만 사용, 원본은 서버 로그로
- **서버·DB에 아무것도 저장 안 함** — 새로고침하면 사라지는 것이 정상
- **"합격/불합격" 자동 판정 문구 금지** — 판단은 사용자 몫
- **계열은 한 번에 1개만** 추출
- **파일 삭제 금지** — `trash-can/`으로 옮기기만

---

## 7. 실행·확인 방법

```bash
cd my-app
npm run dev
```

| 주소 | 용도 |
|---|---|
| http://localhost:3000 | 앱 |
| http://localhost:3000/test-charts/ | 검증용 그래프 8개 (JPG 내려받기 가능) |

**코드를 고쳤으면 반드시:**

```bash
npx tsc --noEmit
```

그리고 브라우저에서 실제로 열어 확인하세요. CLAUDE.md의 검증 루프대로, 말로만 "됐을 것"이라고 하지 마세요.

---

## 8. 문서 지도

| 문서 | 내용 |
|---|---|
| [CLAUDE.md](CLAUDE.md) | **작업 규칙** — 가장 먼저 읽을 것 |
| [PRD.md](PRD.md) | 기획 (범위·비범위·목표 수치) |
| [PLAN.md](PLAN.md) | 31단계 작업 목록 (30개 완료) |
| [DESIGN.md](DESIGN.md) | 설계 — 화면·데이터 흐름·알고리즘·API. 변경 이력은 Version History |
| [VERIFICATION.md](VERIFICATION.md) | 검증 결과 (오차율·재현율·한계) |
| `public/test-charts/README.md` | 검증용 그래프 8개의 정답 수식 |

### 범위에서 뺀 것 (되살리지 마세요)

**기준값 비교·검토 기능**(구간별 상한/하한, 이탈 강조)은 사용자 결정으로 다음 단계로 보류했습니다.
이번 버전은 **좌표를 뽑아 CSV로 내려받는 것까지**가 범위입니다.
관련 규칙은 CLAUDE.md와 DESIGN.md에 "보류"로 표시되어 남아 있습니다.
