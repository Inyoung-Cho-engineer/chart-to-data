# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 따라야 할 규칙을 정의합니다.

## 프로젝트 개요

**chart-to-data** — PDF·JPG·PNG 형태의 논문/기술 보고서에 실린 라인차트를 인식해 그래프 위 좌표값(데이터 포인트)을 자동 추출하고, 사용자가 입력한 기준값(허용 범위)과 비교·검토할 수 있게 돕는 **설계 검증 보조 도구**입니다.

- 사용자: 설계 검증이 필요한 연구실/팀원, 공학 분야 연구자·개발자
- 상세 기획: [PRD.md](PRD.md) (전체), [prd_lite.md](prd_lite.md) (요약)
- 현재 상태: 기획 단계 완료, 코드 구현 전 (Next.js 프로젝트 미생성)

## 작업 규칙

- **모든 설명과 주석은 한국어로 작성한다.** (대화 응답, 코드 주석, 커밋 메시지, 문서 모두)
- **새 파일은 `my-app` 폴더 안에만 만든다.** 상위 폴더나 바탕화면 등 프로젝트 밖에 파일을 생성하지 않는다.
- **코드를 바꾸면 반드시 "무엇을, 왜 바꿨는지"를 한 줄로 알려준다.**
- **파일 삭제 금지.** 지워야 할 파일이 있으면 프로젝트 안에 `trash-can/` 폴더를 만들어 그 안으로 **옮기기만** 한다. 실제 삭제는 사용자가 직접 확인 후 진행한다. (`trash-can/`도 `.gitignore`에 등록)
- **이미 설치된 서브에이전트를 필요할 때마다 적극 활용한다.** (예: 프론트엔드 구조 설계 → `frontend-architect`, 코드 품질 점검 → `code-analyzer`, 보안 검토 → `security-architect`, 탐색 → `Explore`)

## 작업 절차 (검증 루프)

코드를 만질 때는 **매번 아래 루프를 반복한다.** 한 번에 끝내지 말고, 통과할 때까지 돈다.

1. **변경한다** — 작은 단위로 한 가지씩 수정한다.
2. **결과를 직접 확인한다** — 말로만 "됐을 것"이라고 하지 않는다. 개발 서버를 띄우고 브라우저로 실제 화면을 열어보거나, 해당 코드를 실행해 동작을 눈으로 확인한다. 콘솔·네트워크 오류도 함께 본다.
3. **스스로 코드 리뷰한다** — 방금 바꾼 부분을 다시 읽으며 점검한다.
   - CLAUDE.md의 규칙(비범위, 보안, 데이터 처리)을 어기지 않았는가
   - 버그·예외 처리 누락·불필요하게 복잡한 코드가 없는가
   - 필요하면 `code-analyzer` 서브에이전트를 활용한다
4. **문제가 있으면 고치고 다시 1)로 돌아간다.**
5. **통과하면 무엇을, 왜 바꿨는지 한 줄로 요약해 알려준다.**

확인하지 않은 변경을 "완료"라고 보고하지 않는다. 확인하지 못한 부분이 있으면 그 사실을 솔직히 밝힌다.

**`trace.ts`·`plotBox.ts`의 픽셀 계산 임계값을 고쳤을 때는(2026-08-14 교훈)** 그래프 하나만 보고 "고쳤다"고 판단하지 않는다 — 임계값끼리 서로 얽혀 있어 하나를 고치면 다른 그래프가 깨질 수 있다. `/dev-verify`로 검증용 그래프 전체를 다시 측정해 회귀가 없는지 확인한 뒤에만 완료로 본다.

## 기술 스택

- **Next.js로 고정한다.** PRD에서 정한 스택이므로 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다.
- Next.js 단일 프로젝트로 프론트엔드 + 서버 API(Route Handler)를 모두 처리
  - 차트 추출 전용 모델(LineFormer 등)을 별도 Python 서버로 띄우지 않는다 — 검토 후 제외된 방향이므로 다시 꺼내지 말 것
- 이미지 판독은 **하이브리드 방식**으로 처리한다 (2026-08-13 실측 후 변경)
  - **외부 AI 비전 모델**(OpenAI): 그래프 영역(plotBox) 찾기, 축 눈금·범위·단위 읽기, 계열 목록과 **색상** 알아내기, 교차 구간 찾기
  - **앱(브라우저)**: AI가 알려준 계열 색을 따라가며 선의 위치를 픽셀 단위로 정밀 측정 + 축 변환식 계산
  - 변경 이유: AI에게 좌표까지 읽게 했을 때 실측 오차가 평균 5.9~7.7%(최대 19.8%)로 PRD 목표(±3%)를 못 맞췄고, 같은 그래프에서도 실행마다 값이 달라져 재현율 95%도 불가능했다. 앱이 색으로 픽셀을 추적하면 같은 조건에서 평균 오차 0.1%·최대 0.28%·재현성 100%가 나온다.
  - 새 서버나 차트 전용 모델을 쓰는 것이 아니다(브라우저 Canvas 계산). PRD 비범위의 "LineFormer 별도 Python 서버"와 무관하다.
- **배포는 Vercel을 사용한다.**
- DB 없음 — 서버에 아무 것도 영구 저장하지 않는다 (아래 "데이터 처리 원칙" 참고)

## 핵심 기능과 AI가 반드시 지켜야 할 규칙

### 1) 그래프 좌표 추출 기능
- 이미지가 브라우저에서 외부로 직접 나가면 안 됨 — 비전 모델 호출은 **반드시 서버 쪽 코드(API Route)에서만** 수행
- 차트 영역은 자동 탐지하지 않는다 — **사용자가 드래그로 직접 지정**한 영역만 잘라서 판독에 사용
- 업로드 제한: **20MB 이하, PDF 30페이지 이하** — 초과 시 판독 시도하지 말고 안내 문구만 표시
- 실패 안내는 3단계로 구분해 각기 다른 문구 표시 (원본 에러 메시지를 그대로 노출하지 말 것)
  1. 파일 단계 (형식/용량/페이지 초과)
  2. 영역 단계 (지정 영역에서 라인차트 인식 실패)
  3. 모델 응답 단계 (호출 실패/타임아웃/응답 형식 이상)
- 한 그래프에 선(계열)이 여럿이면 **한 번에 1개 계열만** 선택해 추출 (일괄 추출 금지)
- 좌표 추출 규칙: 선택한 계열의 X축을 균등 간격으로 나눈 **50개 지점** + 사용자가 직접 입력한 X값 추가 질의
- 추출 전 반드시 **축 유형(선형/로그)·범위·단위를 화면에 노출하고 사용자가 확인·수정**한 뒤에만 추출 진행
  - 축 유형을 자동으로 판별하지 못했을 때 임의로 선형이라 가정하지 말 것 — 사용자가 직접 지정하게 한다
  - **그래프 테두리 = 축 눈금 범위라고 가정하지 말 것** (2026-08-14 실측·수정) — 실제 논문 그래프는 데이터 양옆에 여백이 있어 테두리와 눈금 최솟값·최댓값이 다르다. 테두리 기준으로 계산하면 최대 57% 오차가 났다. 반드시 눈금 표시(tick)를 찾아 확인한다(`plotBox.ts`의 `AxisCalibration`)
- 추출값은 항상 원본 그래프의 축 범위를 벗어나면 안 됨 — 벗어나면 걸러내는 검사 필요
- 결과는 반드시 **표/리스트 형태 + 원본 이미지와 나란히 비교** 가능하게 표시
- 각 지점의 **신뢰도(high/medium/low)**를 함께 남기고, low는 "확인 필요"로 표시. 계열이 교차하는 구간도 "확인 필요"로 표시
  - 하이브리드 전환 후 신뢰도는 앱이 픽셀 추적 결과로 정한다 (선을 못 찾음·여러 겹침·교차 구간 등)
- 서버에 결과를 저장하지 않으므로 **CSV 다운로드 기능은 필수**

### 2) 기준값 비교·검토 기능 — **다음 단계로 보류** (2026-08-13 범위 축소)
이번 버전은 좌표를 뽑아 CSV로 내려받는 것까지가 범위다. 아래 규칙은 이 기능을 다시 시작할 때 적용한다.
- 기준값은 **X 구간별로 다르게** 입력 가능 (구간마다 상한/하한) — 구간 미지정 부분은 비교 대상에서 제외
- 기준 이탈 지점은 색상/아이콘 등으로 **명확히 강조**
- **자동으로 합격/불합격 판정을 내리지 않는다** — 항상 사용자가 최종 판단, AI는 보조 정보(수치+근거)만 제공
- 비교 결과는 추출값·기준값·차이를 함께 제시

## 이번 범위에서 만들지 않는 것 (비범위)

임의로 추가하지 말 것:
- 설계 문서/요구사항 지식베이스 자동 연결
- 라인차트 외 다른 그래프 유형(막대, 산점도, 파이차트 등) 인식
- 여러 계열 일괄 추출
- 기준값 자동 판정(합격/불합격 자동 결론)
- 회원가입/로그인, 협업/공유 기능, 권한 관리
- 다국어 지원, 모바일 대응
- 대량 파일 일괄 처리

## 데이터 처리 원칙

- 업로드 파일, 추출 결과, 비교 결과 모두 **서버/DB에 저장하지 않는다** — 브라우저 세션에만 존재, 새로고침 시 사라짐
- 검증 근거 보관은 사용자가 CSV로 직접 다운로드하는 방식으로만 제공
- 사용 대상 문서는 **공개 자료(논문/기술 보고서)** 로 한정 — 이미지가 외부 비전 모델로 전송되기 때문. 대외비/미공개 문서 업로드를 전제로 한 기능을 만들지 말 것
- 개인정보를 다루지 않는 전제이므로 회원 식별 정보를 저장/수집하는 기능을 추가하지 말 것

## 보안 / 환경변수

- 모든 API 키는 `.env`에만 보관하고 코드에 하드코딩하지 않는다
- **`.env` 등 비밀 정보 파일과 `node_modules/`는 `.gitignore`에 등록해 두고 절대 커밋하지 않는다.** (현재 등록되어 있음 — 유지할 것)
- 키를 쓰는 호출(OpenAI 비전 모델 등)은 **서버 쪽 코드에서만** 수행, 브라우저로 절대 노출 금지
- **토큰 값을 사용자에게 묻거나 채팅/로그에 출력하지 않는다.** 인증이 필요하면 `.env`에 있는 값을 읽어서 사용한다.
  - Supabase 작업이 필요하면 → Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 인증
  - Vercel 작업(배포 등)이 필요하면 → Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증
  - GitHub 작업이 필요하면 → `.env`의 `GITHUB_TOKEN` 사용
- 배포 시 키는 Vercel 환경변수 설정에 등록 (코드/저장소에 포함 금지)

## 개발 순서 (권장)

PRD.md 9장의 개발 단위를 따른다. 큰 흐름:
1. Next.js 기본 화면 → env 설정
2. 업로드 → 이미지 표시 → 영역 드래그 지정 → 서버 전송 API
3. 비전 모델 호출 → 축 정보 추출/사용자 수정 UI → 계열 선택 → 좌표 추출(50개 지점 + 신뢰도) → 표/원본 이미지 비교
4. 기준값 구간 입력 → 이탈 지점 비교/강조 → CSV 다운로드
5. 3단계 실패 안내 문구 → 검증용 그래프로 오차율(±3%)·재현율(95%) 목표 확인

작은 기능 단위로 순서대로 구현하고, 한 번에 너무 많은 기능을 욕심내지 않는다.
