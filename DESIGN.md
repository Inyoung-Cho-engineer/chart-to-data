# DESIGN.md — chart-to-data 설계서

> **한 줄 요약**: 브라우저가 이미지를 들고 있고, AI에게는 "무엇이 어디 있는지"(그래프 영역·축·계열 색)만 물어본 뒤, 선을 따라가는 정밀 측정과 값 계산은 앱이 직접 한다.
>
> - 상위 문서: [PRD.md](PRD.md) → [PLAN.md](PLAN.md) → **DESIGN.md**
> - 규칙: [CLAUDE.md](CLAUDE.md)
> - 작성 기준일: 2026-08-13 / 상태: 초안

---

## Context Anchor

| 키 | 값 |
|---|---|
| **WHY** | 논문·보고서 라인차트의 수치를 눈으로 읽어 옮겨 적는 수작업이 느리고, 사람마다 값이 달라 검증 근거가 남지 않음 |
| **WHO** | 설계 검증이 필요한 연구실 구성원·팀원, 공학 분야 연구자·개발자 |
| **RISK** | 비전 모델 판독이 흔들려 ±3% 오차·95% 재현율을 못 맞출 위험 / 이미지가 브라우저에서 외부로 새어 나갈 위험 |
| **SUCCESS** | 오차 ±3% 이내, 재추출 일치율 95% 이상, 그래프 1개당 3분 이내 |
| **SCOPE** | 라인차트 1개 · 계열 1개씩 · 좌표 추출 + CSV. 기준값 비교·검토는 다음 단계로 보류(PRD 6장 비범위 참고) |

---

## 1. 설계 목표와 원칙

### 1.1 설계 목표

1. **AI에게는 "무엇이 어디 있는지"만 맡긴다** — AI는 그래프 영역·축 눈금·계열 색상·교차 구간을 알아내고, 선을 따라가는 정밀 측정과 값 계산은 앱이 한다. 재현성(95%)과 정확도(±3%)의 근거. (§2 참고 — 처음에는 좌표 판독까지 AI에게 맡겼으나 실측에서 목표 미달로 방식을 바꿨다)
2. **원본 파일은 브라우저 밖으로 내보내지 않는다** — 서버로 가는 것은 사용자가 드래그로 지정한 영역 조각 하나뿐.
3. **아무것도 저장하지 않는다** — 서버·DB 저장 없음. 새로고침하면 사라지고, 남기고 싶으면 CSV로 내려받는다.
4. **AI가 확정하지 않는다** — 축 유형·범위, 계열 선택, 합격 여부는 전부 사용자가 확인·결정한다.

### 1.2 설계 원칙

- 실패는 **파일 / 영역 / 모델 응답** 세 단계로만 분류하고, 원본 에러 메시지는 화면에 절대 노출하지 않는다.
- 사용자가 확인하지 않은 값으로는 다음 단계로 넘어갈 수 없게 화면에서 막는다.
- 서버는 상태를 기억하지 않는다(stateless). 필요한 것은 매 호출마다 브라우저가 함께 보낸다.

---

## 2. 설계안 비교와 선택

| 기준 | A안: AI가 값까지 | B안: AI가 위치, 앱이 변환 | **D안: 하이브리드 (선택)** |
|---|:-:|:-:|:-:|
| AI에게 묻는 것 | 완성된 숫자값 50개 | 선이 지나가는 위치(ty) | **그래프 영역·축·계열 색상·교차 구간** |
| 선을 따라가는 측정 | AI | AI | **앱 (색상 기준 픽셀 추적)** |
| 값 변환 계산 | AI | 앱 | **앱** |
| **실측 평균 오차** | 30~50% | **5.9~7.7%** (최대 19.8%) | **0.10%** (최대 0.28%) |
| **±3% 이내 지점** | 거의 없음 | 13~19 / 50 | **50 / 50** |
| 재현성(95% 목표) | 낮음 | 실행마다 달라짐 | **100% (계산이므로 항상 동일)** |
| AI 호출 횟수 | 6회(분할) | 6회(분할) | **1회 (analyze만)** |

**선택: D안 (하이브리드)** — 2026-08-13, 정답을 아는 검증용 그래프로 실제 측정한 뒤 B안에서 변경.

- **왜 B안을 버렸나**: PRD 3장은 ±3% 오차와 95% 재현율을 요구한다. B안으로 최선을 다해 튜닝했는데도(그래프 영역만 잘라 전달, 상위 모델, 분할 질의, 온도 0, 판독용 눈금 격자) 평균 오차 5.9~7.7%에 머물렀고, 같은 이미지인데 실행마다 값이 달라졌다. 비전 모델이 픽셀 단위 정밀 판독에 근본적으로 약한 것이 원인이라 프롬프트로는 해결되지 않았다.
- **D안이 하는 일**: AI가 알려준 계열 색상을 기준으로, 앱이 그래프 영역의 각 세로줄을 훑어 그 색의 선이 있는 높이를 찾는다. 그 위치를 §5.3 변환식에 넣어 실제 값을 만든다.
- **AI를 빼는 것이 아니다**: 그래프 영역(plotBox) 찾기, 축 눈금·범위·단위 읽기, 계열 목록과 색상 알아내기, 교차 구간 찾기는 여전히 AI가 한다. 사람 눈에 의존하던 "무엇이 어디 있는지"는 AI가, 자로 재는 일은 앱이 맡는 분업이다.
- **비범위와 무관**: 새 서버나 차트 추출 전용 모델(LineFormer 등)을 쓰지 않는다. 브라우저 Canvas 계산만 쓰므로 "Next.js 한 프로젝트" 원칙을 그대로 지킨다.
- **한계**: 계열 색이 뚜렷할 때 잘 동작한다. 흑백 점선이나 색이 거의 같은 선들, 선이 겹치는 구간은 정확도가 떨어질 수 있어 "확인 필요"로 표시한다. plotBox가 어긋나면 전체가 밀리므로 축 확인 단계에서 사용자가 바로잡을 수 있어야 한다.

---

## 3. 화면 구성 (무엇이 어디에 보이는지)

### 3.1 전체 배치

화면은 **한 페이지**입니다. 페이지를 옮겨 다니지 않고, 위쪽 단계 표시가 진행 상황을 알려줍니다.

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  chart-to-data                                                              │
│  ①업로드 ─ ②영역지정 ─ ③축확인 ─ ④계열선택 ─ ⑤추출                          │
│  ⓘ 작업 내용은 서버에 저장되지 않습니다. 새로고침하면 사라집니다.             │
├──────────────────────────────────┬──────────────────────────────────────────┤
│  [왼쪽 · 약 55%] 원본 이미지      │  [오른쪽 · 약 45%] 지금 할 일 패널        │
│                                  │                                          │
│  ┌────────────────────────────┐  │   현재 단계에 필요한 입력칸·버튼만        │
│  │                            │  │   여기에 나타납니다.                      │
│  │   업로드한 그림            │  │   (단계별 내용은 3.2 참고)                │
│  │    ┌ ─ ─ ─ ─ ─ ┐          │  │                                          │
│  │    │ 드래그 영역 │         │  │                                          │
│  │    └ ─ ─ ─ ─ ─ ┘          │  │                                          │
│  └────────────────────────────┘  │                                          │
│   ◀ 3 / 12 페이지 ▶  [영역 다시 잡기]│                                       │
├──────────────────────────────────┴──────────────────────────────────────────┤
│  [아래 · 전체 폭] 결과 표  — ⑤단계부터 나타납니다                            │
│  X값 │ 추출값 │ 신뢰도 │ 상태                              [CSV 내려받기]     │
└─────────────────────────────────────────────────────────────────────────────┘
```

- **왼쪽 이미지는 처음부터 끝까지 계속 보입니다.** PRD의 "추출 결과를 원본 이미지와 나란히 비교"를 이 배치로 만족시킵니다.
- 오른쪽 패널은 현재 단계 것만 열려 있고, 지난 단계는 접힌 요약 줄로 남아 언제든 되돌아갈 수 있습니다.
- 실패 안내는 오른쪽 패널 위쪽에 노란/빨간 띠로 나타납니다.

### 3.2 단계별 오른쪽 패널 내용

| 단계 | 오른쪽 패널에 보이는 것 | 왼쪽 이미지 영역의 변화 |
|---|---|---|
| ① 업로드 | 파일 고르기 버튼, 제한 안내("20MB 이하, PDF 30페이지 이하") | 비어 있음 |
| ② 영역 지정 | "그래프를 드래그로 감싸주세요" 안내, 잘라낸 영역 미리보기, `[이 영역으로 분석]` | 드래그하면 반투명 사각형이 그려짐 |
| ③ 축 확인 | X축·Y축 각각 — 유형(선형/로그 라디오), 최솟값·최댓값 입력칸, 단위 입력칸, `[확인했습니다]` | 인식된 그래프 안쪽 영역이 점선으로 표시 |
| ④ 계열 선택 | 인식된 선 목록(색 견본 + 이름), **라디오 버튼으로 1개만** 선택 | 선택한 계열의 색 견본을 패널에 크게 표시 (원본 이미지 위 실시간 강조는 이번 범위 밖) |
| ⑤ 추출 | `[좌표 50개 추출]` 버튼, 진행 표시, 추출 후 요약(확인 필요 N개), `[CSV 내려받기]` | 변화 없음 |
| (상시) | 특정 X값 추가 질의 입력칸 (⑤ 이후 계속 사용 가능) | — |

### 3.3 결과 표 (아래 영역)

| 열 | 내용 | 표시 규칙 |
|---|---|---|
| # | 순번 (1~50, 추가 질의는 별표) | 추가 질의 행은 배경색 구분 |
| X | X축 값 (단위 함께) | |
| 추출값 | Y축 값 (단위 함께) | |
| 신뢰도 | high / medium / low | low는 회색 배지 |
| 상태 | 정상 / **확인 필요** | 신뢰도 low, 계열 교차 구간, 축 범위 밖 → "확인 필요" + 사유 툴팁 |

> 기준값과의 비교·이탈 표시는 이번 범위에 없습니다(다음 단계로 보류). 이 표는 추출값과 신뢰도를 확인하는 용도까지만입니다.

### 3.4 화면 요소 체크리스트 (구현 시 확인용)

**① 업로드 패널**
- [ ] 파일 선택 버튼 (accept: `.pdf, .jpg, .jpeg`)
- [ ] 제한 안내 문구 (20MB / 30페이지)
- [ ] 서버 미저장 안내 문구
- [ ] "지정한 그래프 영역은 판독을 위해 외부 AI 모델로 전송됩니다. 공개된 논문·기술 보고서 등 공개 자료만 올려주세요" 안내 문구

**② 영역 지정**
- [ ] 이미지 위 드래그 사각형 (마우스 누름→이동→뗌)
- [ ] 선택 영역 픽셀 크기 표시, 너무 작으면(가로·세로 100px 미만) 버튼 비활성
- [ ] 잘라낸 영역 미리보기 썸네일
- [ ] PDF 다중 페이지일 때 페이지 이동 버튼

**③ 축 확인**
- [ ] X축: 유형 라디오(선형/로그), 최솟값, 최댓값, 단위
- [ ] Y축: 유형 라디오(선형/로그), 최솟값, 최댓값, 단위
- [ ] 자동 판별 실패 시 라디오 미선택 상태 + "직접 골라주세요" 문구, 고르기 전 다음 단계 버튼 비활성
- [ ] "AI가 읽은 값입니다. 맞는지 확인하고 필요하면 고쳐주세요" 안내
- [ ] 최솟값 ≥ 최댓값이거나 로그축에 0 이하 값 입력 시 경고

**④ 계열 선택**
- [ ] 계열 목록 (색 견본 + 라벨), 라디오(단일 선택)
- [ ] "한 번에 한 개씩 추출합니다. 다른 선은 나중에 다시 고르면 됩니다" 안내

**⑤ 추출**
- [ ] 추출 버튼 + 진행 중 표시
- [ ] 특정 X값 추가 질의 입력칸 + 추가 버튼
- [ ] 입력한 X값이 확정된 축 범위(min~max) 밖이면 질의를 보내지 않고 "축 범위 안의 값을 입력해주세요" 경고 표시
- [ ] CSV 내려받기 버튼 (추출값·신뢰도·확인 필요 여부 포함)

---

## 4. 데이터 흐름 (입력 → 처리 → 출력)

### 4.1 큰 그림

```
        [브라우저 — 원본 파일은 여기서만 존재]              [서버 — 아무것도 저장 안 함]        [외부]

 PDF/JPG ──▶ 검사(형식·용량·페이지)
              │
              ├─(PDF)─▶ pdf.js로 페이지를 그림으로 변환
              │
              ▼
          화면에 표시 ──▶ 사용자 드래그 ──▶ Canvas로 영역 잘라내기
                                              │  (긴 변 1600px로 축소, JPEG로 압축)
                                              ▼
                                     ┌───▶ POST /api/analyze ──────────▶ OpenAI 비전 모델
                                     │         (잘라낸 조각만 전송)   ◀── 축 후보·계열 목록·
                                     │                                   그래프 안쪽 영역 위치
                                     ◀── 형식 검사(Zod) 통과분만 반환
                                              │
                              사용자가 축 확인·수정 → 계열 1개 선택
                                              │
                    ┌─────────────────────────┴─────────────────────────┐
                    │  여기서부터는 서버·외부 모델을 쓰지 않는다.        │
                    │  아래 ★는 전부 브라우저 안의 계산이다.            │
                    └─────────────────────────┬─────────────────────────┘
                                              │
                            ★ plotBox 영역만 잘라 픽셀 데이터로 읽기
                                              │
                            ★ X축 균등 50지점(tx) 계산 (5.3)
                                              │
                            ★ 각 tx의 세로줄을 훑어, AI가 알려준 계열 색과
                              가장 가까운 픽셀 위치(ty)를 찾기 (5.5)
                                              │
                            ★ 실제 숫자값 계산 (5.3 변환식, tx·ty → x·y)
                                              │
                            ★ 선을 못 찾음·굵게 뭉침·교차 구간을
                              "확인 필요"로 표시
                                              ▼
                                        결과 표 화면 출력
                                              │
                                        CSV 파일로 내려받기
```

★ 표시는 **AI가 아니라 앱의 계산 코드**가 하는 일입니다. 여기가 정확도(±3%)와 재현성(95%)의 핵심입니다.
외부 모델 호출은 위쪽 `/api/analyze` **한 번뿐**입니다.

### 4.2 각 단계의 입력 → 처리 → 출력

| # | 입력 | 처리 | 출력 |
|---|---|---|---|
| 1 | 사용자가 고른 PDF/JPG | 확장자·바이트 크기·(PDF는) 페이지 수 확인 | 통과 또는 파일 단계 안내 |
| 2 | 통과한 파일 | PDF면 선택 페이지를 그림으로 변환 | 화면에 뜬 이미지 |
| 3 | 드래그 좌표 4개 | Canvas로 해당 사각형만 잘라 축소·압축 | 잘라낸 이미지 조각(수백 KB) |
| 4 | 조각 | 서버가 비전 모델에 "축 눈금·계열·그래프 안쪽 영역(plotBox)·교차 위치" 질의 → 응답 형식 검사 | 축 후보 + 계열 목록 + plotBox + 교차 위치 |
| 5 | 축 후보 | 화면에 띄우고 사용자가 확인·수정 | **확정된 축 정보** (서버로는 보내지 않고 브라우저에만 보관) |
| 6 | 계열 목록 | 사용자가 1개 선택 | 선택 계열 id |
| 7 | 조각 + plotBox | **앱이** plotBox 영역만 잘라 Canvas 픽셀 데이터로 읽음 | 그래프 영역 픽셀 |
| 8 | 확정된 X축 범위 | **앱이 X축 균등 50지점의 위치(tx)를 계산** (§5.3) | tx 리스트 50개 |
| 9 | 픽셀 + tx 50개 + 계열 색상 | **앱이 각 tx의 세로줄을 훑어 계열 색과 가장 가까운 픽셀 위치를 찾음** (§5.5) | ty·신뢰도 50쌍 |
| 10 | tx·ty 50쌍 + 확정 축 | **앱이 변환식으로 실제 값 계산** | 좌표값 50개 |
| 11 | 좌표값 | 축 범위 밖·선 못 찾음·교차 구간을 "확인 필요"로 표시(행 유지) | 표에 뿌릴 최종 데이터 |
| 12 | 표 데이터(X값·추출값·신뢰도·확인 필요 여부) | 상단에 주석 헤더(파일명·계열명·축 유형/범위/단위·추출 시각) 5줄을 붙이고, 쉼표로 이어 붙여 텍스트 파일 생성 | CSV 다운로드 |

---

## 5. 데이터 모델과 계산

### 5.1 핵심 타입

```typescript
// 축 하나의 정보 — AI가 제안하고 사용자가 확정한다
interface AxisInfo {
  type: 'linear' | 'log' | 'unknown';  // unknown이면 사용자가 직접 골라야 함
  min: number;
  max: number;
  unit: string;
  confirmedByUser: boolean;            // 이게 true가 아니면 추출 단계로 못 넘어감
}

// 그래프 안의 선 하나
interface Series {
  id: string;
  label: string;        // 범례에서 읽은 이름 (없으면 "계열 1")
  colorHex: string;     // 화면에 견본으로 보여줄 색
}

// AI가 돌려주는 원자료 — plotBox 안에서의 상대 위치만 담는다 (0~1)
// 좌표 기준: plotBox는 이미지(잘라낸 조각) 좌상단이 (0,0)인 정규화 좌표 (x0=왼쪽, y0=위쪽 / x1=오른쪽, y1=아래쪽)
interface RawPoint {
  tx: number;                            // 앱이 미리 계산해 요청에 실은 값을 그대로 붙여 보관 (0=plotBox 왼쪽, 1=오른쪽)
  ty: number;                            // 모델이 답한 값. 축 관례대로 0=plotBox 아래쪽, 1=위쪽
                                          // 환산식: ty = (y1_pix − yPix) / (y1_pix − y0_pix)
  confidence: 'high' | 'medium' | 'low';
}

// 앱이 변환식을 적용해 만든 최종 좌표
interface DataPoint {
  tx: number;                            // plotBox 내 가로 위치 (0~1) — 계열 교차 판정에 사용
  x: number | null;                      // 실제 X값. 범위를 크게 벗어나 계산하지 않은 경우 null
  y: number | null;                      // 실제 Y값. 범위를 크게 벗어나 계산하지 않은 경우 null
  confidence: 'high' | 'medium' | 'low';
  needsCheck: boolean;
  checkReason?: 'low_confidence' | 'crossing' | 'out_of_range';
  source: 'grid' | 'user_query';         // 균등 50지점인지, 사용자가 추가로 물어본 값인지
}

```

> `Threshold`·`ComparisonRow`(기준값 구간·비교 결과) 타입은 기준값 비교 기능과 함께 다음 단계로 보류합니다.

### 5.2 브라우저에 보관하는 작업 상태

```typescript
interface SessionState {
  file: { name: string; pageCount: number; raw: File } | null;
                                  // raw: 원본 File 객체. 서버로 보내지 않고 페이지 다시 그릴 때만 씀
  pageImage: string | null;      // 화면에 띄운 페이지 그림 (원본 페이지 전체)
  currentPage: number;           // 지금 보고 있는 페이지 (1부터 시작, PDF 아니면 항상 1)
  cropRect: { x0: number; y0: number; x1: number; y1: number } | null;
                                  // pageImage 기준 드래그 영역 (정규화 좌표, 좌상단이 0,0)
                                  // 원본 이미지 위에 plotBox·강조를 다시 그릴 때 이 값으로 좌표를 되돌린다
  cropImage: string | null;      // 잘라낸 영역 조각 (API 호출마다 함께 보냄)
  xAxis: AxisInfo | null;
  yAxis: AxisInfo | null;
  plotBox: { x0: number; y0: number; x1: number; y1: number } | null;
                                  // cropImage 기준 그래프 안쪽 영역 (정규화 좌표, 좌상단이 0,0 — x0=왼쪽/y0=위쪽, x1=오른쪽/y1=아래쪽)
  seriesList: Series[];
  selectedSeriesId: string | null;
  crossings: Array<{ tx: number; seriesIds: string[] }>;  // 계열이 교차하는 plotBox 내 가로 위치와 그 두 계열
  points: DataPoint[];
  error: AppError | null;
}
```

> 이 상태는 **브라우저 메모리에만** 있습니다. 새로고침하면 전부 사라지고, 서버에는 어떤 흔적도 남지 않습니다.

### 5.3 위치 ↔ 실제 값 변환식 (앱이 직접 계산)

`t`는 그래프 안쪽 영역(plotBox)에서의 상대 위치(0~1)입니다.

**정방향 — 위치 → 값 (추출 결과를 계산할 때 사용)**
```
선형 축:  값 = min + (max − min) × t

로그 축:  값 = 10 ^ ( log10(min) + (log10(max) − log10(min)) × t )
```

**역방향 — 값 → 위치 (사용자가 입력한 특정 X값을 질의할 때 사용)**
```
선형 축:  t = (값 − min) / (max − min)

로그 축:  t = ( log10(값) − log10(min) ) / ( log10(max) − log10(min) )
```
입력한 값이 확정된 축 범위(`min`~`max`) 밖이면 픽셀 추적을 하지 않고 "축 범위 안의 값을 입력해주세요" 경고를 띄웁니다.

- 로그 축인데 `min ≤ 0`이면 계산이 불가능하므로, 축 확인 단계에서 경고를 띄우고 진행을 막습니다.
- **X축 50지점(tx)**: 그래프 화면상 가로를 49등분한 50개 지점(`tx = 0, 1/49, 2/49, … , 1`)을 앱이 계산합니다. 로그 축이면 값 간격은 로그 균등이 됩니다.
- **plotBox·ty의 좌표 기준**: `plotBox`는 이미지(잘라낸 조각) 좌상단을 (0,0)으로 하는 정규화 좌표입니다(`y0`=플롯 영역 위쪽, `y1`=아래쪽 — 픽셀 좌표계와 동일). `ty`는 축 관례를 따라 아래=0·위=1이므로 `ty = (y1_pix − yPix) / (y1_pix − y0_pix)`로 환산합니다. `tx`는 `x0`=왼쪽·`x1`=오른쪽 그대로 `tx = (xPix − x0_pix) / (x1_pix − x0_pix)`입니다.
- **범위 밖 처리**: `ty`가 `[−0.02, 1.02]`를 벗어나면 오독으로 보고 그 지점의 `x`·`y`를 `null`로 두되 **표에서 행을 지우지 않고 유지**하며 `checkReason = 'out_of_range'`를 붙입니다. `[−0.02, 0)`과 `(1, 1.02]` 경계는 0 또는 1로 잘라(clamp) 값을 계산하고 동일하게 "확인 필요"를 붙입니다.
- **교차 구간**: `crossings` 중 현재 `selectedSeriesId`가 `seriesIds`에 포함된 항목만 대상으로, 그 `tx` ±0.02 안쪽에 들어오는 지점에 "확인 필요(교차)"를 붙입니다.

> **5.4 기준값 비교 계산**은 기준값 비교 기능과 함께 다음 단계로 보류합니다. (이전 버전에서 정의했던 구간 매칭·차이 계산 로직은 이 기능을 다시 시작할 때 참고합니다.)

### 5.5 계열 색상 픽셀 추적 (앱이 직접 수행)

AI가 알려준 계열 색상(`colorHex`)을 기준으로, 앱이 그래프 영역의 각 세로줄에서 그 선의 높이를 찾습니다.
`src/lib/trace.ts`가 담당하며, 서버·외부 모델을 전혀 쓰지 않습니다.

**순서**
1. `cropImage`에서 `plotBox` 영역만 잘라 Canvas에 그리고 픽셀 데이터를 읽는다.
2. 각 `tx`마다 해당 세로줄(그리고 좌우 ±1픽셀)을 위에서 아래로 훑으며, **그 픽셀이 이 계열의 선인지 판별**한다(아래 A).
3. 아무것도 못 찾으면 **좌우로 조금씩 넓혀가며** 다시 찾는다(아래 B — 점선 대응).
4. 후보들이 연속으로 이어진 덩어리(선 두께)로 묶는다.
5. 여러 덩어리가 잡히면 **앞뒤 지점과 이어지는 것**을 고른다(아래 C — 범례·교차 대응).
6. 고른 덩어리의 중앙을 픽셀 행 번호로 보고 `ty`로 환산한다: `ty = 1 − row / (높이 − 1)`

#### A. 계열 판별 — "가장 가까운 색"으로 정한다

계열 색과의 거리가 임계값 이하인지만 보면 안 됩니다. 실측에서 **JPEG 압축으로 흐려진 점선**
(주황 `255,127,14` → `211,148,79`)이 20곳 중 18곳에서 걸러졌습니다. 그렇다고 임계값을 올리면
회색 축선(`118,118,118`)까지 선으로 잡힙니다.

그래서 **후보 색들 중 우리 계열 색이 가장 가까울 때만** 인정합니다.
- 후보: 선택한 계열 색 + **다른 계열들의 색**(AI가 알려줌) + 배경·축 색(흰색, 검정, 회색 2종)
- 거리가 260(맨해튼)을 넘으면 아예 다른 색으로 보고 버린다

> 흐려진 주황 `(211,148,79)`은 주황까지 130, 회색까지 152 → **주황이 더 가까우므로 선으로 인정**.
> 회색 축선 `(118,118,118)`은 주황까지 250, 회색까지 30 → **회색이 더 가까우므로 제외**.

#### B. 점선 대응 — 못 찾으면 좌우로 넓힌다

점선은 세로줄이 빈 칸에 걸리면 아무것도 못 찾습니다(실측: 50지점 중 43개 실패).
±1픽셀에서 시작해 2픽셀씩, **최대 플롯 폭의 2%(최소 10픽셀)까지** 넓혀가며 가장 가까운 선 조각을 찾습니다.

#### C. 연속성 추적 — 앞뒤 지점과 이어지는 것을 고른다

그래프 **안쪽에 그려진 범례 선**이나 다른 계열과 겹치는 구간에서, 한 지점만 따로 보면 엉뚱한 선을
고를 수 있습니다(실측: 범례를 데이터 선으로 착각해 최대 28% 오차).

1. **시작점(anchor)**: 덩어리가 하나뿐이라 헷갈릴 여지가 없는 지점 중 가운데에 가장 가까운 것.
2. anchor에서 **좌우로 퍼져나가며**, 직전에 고른 위치에 가장 가까운 덩어리를 고른다.
3. 가장 가까운 후보조차 **플롯 높이의 20%를 넘게 튀면** 값을 내놓지 않고 "확인 필요"로 남긴다.
   틀린 숫자를 주는 것보다 낫다는 판단이다.

**신뢰도 판정** (모델이 아니라 이 규칙이 정한다)
| 상황 | 신뢰도 | 표시 |
|---|---|---|
| 덩어리 하나만 뚜렷하게 잡힘 | `high` | 정상 |
| 덩어리가 여럿(다른 선과 겹침 등)이거나 지나치게 두꺼움 | `medium` | 정상 |
| 그 세로줄에서 계열 색을 찾지 못함 / 앞뒤와 너무 동떨어짐 | `low` | 확인 필요(선 못 찾음) |
| `crossings`가 알려준 교차 구간 ±0.02 안 | 그대로 | 확인 필요(교차) |

**임계값** — 실측으로 정한 기본값이며, 그래프가 흐릿하면 조정이 필요할 수 있습니다.
| 값 | 기본 | 뜻 |
|---|---|---|
| 최대 색 거리 | 260 | 이보다 멀면 아예 다른 색 |
| 세로줄 기본 폭 | ±1픽셀 | 얇은 선을 놓치지 않기 위함 |
| 넓히기 상한 | 플롯 폭의 2% (최소 10픽셀) | 점선 빈 칸을 덮을 만큼 |
| 과도한 두께 | 플롯 높이의 15% 초과 | 뭉친 덩어리 → `medium` |
| 최대 도약 | 플롯 높이의 20% | 넘으면 "확인 필요"로 남김 |

**2계열(실선+점선) 실측** — 개선 전후

| 계열 | 개선 전 | 개선 후 |
|---|---|---|
| 실선 | 50/50 찾음, 최대 오차 **28.13%** (범례 오인) | 50/50 찾음, 평균 0.23%·최대 **1.16%** |
| 점선 | **7/50** 찾음 (43개 "확인 필요") | **48/50** 찾음, 평균 0.60%·최대 **2.93%** |

두 계열 모두 ±3%를 넘는 지점이 없습니다. 못 찾은 2지점은 값을 내놓지 않고 "확인 필요"로 남습니다.

### 5.6 plotBox 픽셀 보정 (앱이 직접 수행)

**왜 필요한가**: AI는 `plotBox`를 `{0.1, 0.1, 0.9, 0.9}`처럼 반올림한 값으로 대충 답하는 일이 잦습니다.
실측에서 아래쪽 경계가 정답보다 **7.9% 어긋났고**, 그 오차가 추출값 오차(최대 8.09%)로 그대로 옮겨졌습니다.
§5.5 픽셀 추적이 아무리 정확해도 **기준이 되는 사각형이 틀리면 전체가 밀리므로**, 축 선을 픽셀에서
직접 찾아 AI 값을 바로잡습니다. `src/lib/plotBox.ts`가 담당합니다.

**순서**
1. `cropImage`의 각 열·행에서 어두운 픽셀(밝기 128 미만) 개수를 센다.
2. 가장자리 40% 안쪽까지 훑어, 절반 이상이 어두운 줄을 **축 선**으로 본다. (왼쪽·오른쪽 세로선, 위·아래 가로선)
3. 축이 L자 모양이라 위·오른쪽 선이 없으면, **찾은 축 선이 어디서 어디까지 뻗어 있는지**를 재서 나머지 경계를 얻는다.
   (아래 축 선의 가로 구간 → `x0`·`x1`, 왼쪽 축 선의 세로 구간 → `y0`·`y1`)
4. 어느 변도 못 찾으면 그 변만 AI가 준 값을 그대로 쓴다. 결과가 뒤집히거나 지나치게 좁으면 보정 전체를 포기한다.

> **눈금(tick)으로 찾는 방법은 실패했습니다.** 1px 눈금은 JPEG 압축을 거치면 밝기 224~241로 거의
> 하얗게 날아가 감지되지 않았습니다. 반면 축 선은 뚜렷하게 남아 있어 3번 방식이 훨씬 안정적입니다.

**보정 효과 (같은 검증 그래프 실측)**

| plotBox | 평균 오차 | 최대 오차 | ±3% 이내 |
|---|---|---|---|
| AI가 준 값 그대로 | 4.08% | 8.09% | 19/49 |
| 축 선 감지만 (L자, 오른쪽 미보정) | 1.27% | 2.97% | 50/50 |
| **축 선 감지 + 뻗은 구간으로 나머지 보정** | **0.083%** | **0.222%** | **50/50 (±1% 이내도 50/50)** |

---

## 6. 서버 API 명세

서버는 **아무것도 기억하지 않습니다.** 이미지 조각을 매 호출마다 브라우저가 함께 보냅니다.
**하이브리드 전환(§2 D안) 이후 외부 모델을 부르는 경로는 `/api/analyze` 하나뿐입니다.** 좌표 추출은 브라우저가 §5.5 픽셀 추적으로 직접 하므로 서버를 거치지 않습니다.

| 방식 | 경로 | 하는 일 |
|---|---|---|
| POST | `/api/analyze` | 조각에서 축 정보·계열 목록(색상 포함)·그래프 안쪽 영역·교차 위치를 읽어옴 |

> 이전 버전의 `/api/extract`·`/api/point`는 제거되었습니다. 좌표 추출·특정 X값 조회 모두 `src/lib/trace.ts`가 브라우저에서 처리합니다.

### 6.1 `POST /api/analyze`

**요청**
```json
{ "image": "data:image/jpeg;base64,...." }
```

**응답 (200)**
```json
{
  "plotBox": { "x0": 0.12, "y0": 0.08, "x1": 0.97, "y1": 0.91 },
  "xAxis": { "type": "linear", "min": 0, "max": 100, "unit": "mm" },
  "yAxis": { "type": "log", "min": 0.001, "max": 1, "unit": "MPa" },
  "series": [
    { "id": "s1", "label": "Case A", "colorHex": "#1f77b4" },
    { "id": "s2", "label": "Case B", "colorHex": "#d62728" }
  ],
  "crossings": [
    { "tx": 0.42, "seriesIds": ["s1", "s2"] },
    { "tx": 0.78, "seriesIds": ["s1", "s2"] }
  ]
}
```
- `crossings`는 어느 두 계열이 어디서 교차하는지를 함께 담습니다. 계열이 3개 이상이면 관련 없는 교차가 선택한 계열의 "확인 필요" 판정에 섞이지 않도록 하기 위함입니다(§5.3).
- 축 유형을 읽지 못하면 `"type": "unknown"`으로 돌려주고, 화면에서 사용자가 직접 고르게 합니다.
- 라인차트를 못 찾으면 `REGION_NO_CHART` 오류로 응답합니다(영역 단계 실패).

### 6.2 오류 응답 형식

```json
{ "error": { "code": "MODEL_TIMEOUT", "stage": "model" } }
```
- 서버는 **코드와 단계만** 돌려줍니다. 화면에 보여줄 문구는 브라우저가 7장의 표에서 골라 씁니다.
- 모델이 보낸 원본 에러 메시지는 서버 로그에만 남기고 응답에 절대 담지 않습니다.

**코드별 HTTP 상태 코드**

| 코드 접두사 | HTTP 상태 |
|---|---|
| `FILE_*` | 400 |
| `REGION_*` | 422 |
| `MODEL_TIMEOUT` | 504 |
| `MODEL_CALL_FAILED` / `MODEL_BAD_FORMAT` | 502 |

---

## 7. 실패 처리 (3단계)

| 단계 | 코드 | 언제 | 화면 문구 |
|---|---|---|---|
| 파일 | `FILE_TYPE` | PDF·JPG가 아님 | "PDF 또는 JPG 파일만 올릴 수 있습니다. 다른 형식이면 이미지로 저장한 뒤 다시 올려주세요." |
| 파일 | `FILE_SIZE` | 20MB 초과 | "파일이 20MB를 넘습니다. 필요한 페이지만 따로 잘라서 올려주세요." |
| 파일 | `FILE_PAGES` | PDF 30페이지 초과 | "PDF가 30페이지를 넘습니다. 그래프가 있는 페이지만 따로 저장해 올려주세요." |
| 영역 | `REGION_TOO_SMALL` | 지정 영역이 너무 작음 | "지정한 영역이 너무 작습니다. 그래프의 축과 눈금까지 함께 감싸도록 조금 넓게 드래그해주세요." |
| 영역 | `REGION_NO_CHART` | 라인차트를 못 찾음 | "이 영역에서 라인차트를 찾지 못했습니다. 축과 선이 모두 들어가도록 영역을 다시 잡아주세요." |
| 영역 | `REGION_NO_AXIS` | 축 눈금을 못 읽음 | "축 눈금을 읽지 못했습니다. 축 숫자가 잘리지 않도록 영역을 조금 넓혀서 다시 시도해주세요." |
| 모델 | `MODEL_CALL_FAILED` | 호출 실패 | "판독 요청이 처리되지 않았습니다. 잠시 후 다시 시도해주세요." |
| 모델 | `MODEL_TIMEOUT` | 응답 시간 초과 | "판독에 시간이 너무 오래 걸렸습니다. 영역을 조금 좁게 잡고 다시 시도해주세요." |
| 모델 | `MODEL_BAD_FORMAT` | 응답 형식이 예상과 다름 | "판독 결과를 이해하지 못했습니다. 다시 시도하거나 영역을 다시 잡아주세요." |

- 모든 안내에는 `[다시 시도]` 또는 `[영역 다시 잡기]` 버튼이 함께 붙습니다.
- 문구는 `src/lib/errors.ts` 한 파일에 표로 모아 두고 화면에서는 코드로만 참조합니다.

---

## 8. 기술 선택

### 8.1 기본 — Next.js

| 기술 | 쉬운 설명 | 왜 필요한가 |
|---|---|---|
| **Next.js (App Router)** | 화면과 서버 코드를 한 프로젝트에서 만들고, Vercel에 그대로 올릴 수 있게 해주는 뼈대입니다. | PRD·CLAUDE.md에서 고정한 스택. Part 5의 Vercel 배포가 추가 설정 없이 이어집니다. |
| **TypeScript** | 값이 숫자인지 글자인지 미리 정해 둬서, 실수하면 실행 전에 알려주는 안전장치입니다. | 축·좌표·기준값처럼 숫자를 다루는 앱이라 형식 실수 하나가 계산 오류로 이어집니다. |
| **Tailwind CSS** | 화면 꾸미기를 미리 만들어진 짧은 이름으로 빠르게 하는 도구입니다. | Next.js 설치할 때 선택지로 함께 제공돼 추가 부담이 없습니다. |

### 8.2 추가로 필요한 기술 (제안)

| 기술 | 쉬운 설명 | 왜 필요한가 | Vercel 배포 |
|---|---|---|---|
| **pdf.js** (`pdfjs-dist`) | PDF를 브라우저 안에서 그림으로 바꿔주는 도구입니다. | PDF를 화면에 띄우고 페이지 수를 세려면 필요합니다. **브라우저에서 처리하므로 원본 PDF가 서버로 가지 않습니다.** | 브라우저에서만 도니 서버 부담 없음 ✅ |
| **HTML Canvas** | 브라우저에 원래 들어 있는 그림판 기능입니다. 설치할 것이 없습니다. | 드래그한 영역만 잘라내고, 보내기 전에 크기를 줄이는 일을 합니다. | 설치 불필요 ✅ |
| **OpenAI SDK** (`openai`) | AI에게 이미지를 보내고 답을 받아오는 공식 도구입니다. | 비전 모델 호출을 서버 코드에서만 하기 위해 씁니다. | 서버 함수에서 동작 ✅ |
| **Zod** | AI가 보낸 답이 약속한 형식대로인지 검사해주는 문지기입니다. | 형식이 어긋난 답을 그대로 쓰면 화면이 깨집니다. 검사에 걸리면 "모델 응답 단계 실패"로 바꿔 보여줍니다. | 가볍고 서버·브라우저 모두 동작 ✅ |
| **Zustand** | 여러 화면 조각이 같은 작업 내용(파일·축·추출값)을 함께 보게 해주는 작은 메모장입니다. | 단계가 5개라 상태를 여기저기 넘겨주다 보면 코드가 엉킵니다. **새로고침하면 사라지므로 "저장하지 않는다" 원칙과도 맞습니다.** | 브라우저 안에서만 동작 ✅ |

**일부러 쓰지 않는 것**

| 안 쓰는 것 | 이유 |
|---|---|
| CSV 라이브러리 | 표가 단순해 쉼표로 이어 붙이면 충분합니다. 검증 근거를 남기기 위해 상단에 파일명·계열명·축 정보 등 주석 헤더(§4.2 #12)를 붙입니다. (엑셀에서 한글이 깨지지 않도록 파일 앞에 BOM 표시도 붙입니다) |
| 데이터베이스 | 서버에 아무것도 저장하지 않기로 했으므로 필요 없습니다. |
| 로그인 라이브러리 | 회원 기능이 비범위입니다. |
| 별도 Python 추출 서버 | PRD 6장에서 제외된 방향입니다. |

### 8.3 Vercel 배포에서 미리 대비할 두 가지

| 항목 | 무슨 문제인가 | 이 설계의 대응 |
|---|---|---|
| **서버로 보낼 수 있는 데이터 크기 제한 (약 4.5MB)** | 20MB짜리 파일을 통째로 보내면 배포 후에 실패합니다. | 원본은 아예 안 보냅니다. 잘라낸 영역만 **긴 변 1600px로 줄이고 JPEG로 압축**해 보냅니다(보통 200~500KB). |
| **서버 함수 실행 시간 제한 (기본 10초)** | 비전 모델 판독이 10초를 넘길 수 있습니다. | 각 API 파일에 `export const maxDuration = 60`을 넣고, 55초에 스스로 끊어 `MODEL_TIMEOUT`으로 안내합니다. |

**환경변수**: `OPENAI_API_KEY`는 `.env`에만 두고 서버 코드에서만 읽습니다. `NEXT_PUBLIC_` 접두사를 절대 붙이지 않습니다(붙이면 브라우저로 노출됨). 배포 시에는 Vercel 프로젝트 설정의 환경변수에 등록합니다.

---

## 9. 파일 구조

```
my-app/
├── src/
│   ├── app/
│   │   ├── page.tsx                    # 전체 한 페이지 화면
│   │   ├── layout.tsx
│   │   └── api/
│   │       └── analyze/route.ts        # 축·계열(색상)·plotBox·교차 인식 — 외부 모델을 부르는 유일한 경로
│   ├── components/
│   │   ├── StepIndicator.tsx           # 위쪽 단계 표시
│   │   ├── ImageViewer.tsx             # 왼쪽 이미지 + 드래그 선택
│   │   ├── UploadPanel.tsx             # ① 업로드
│   │   ├── RegionPanel.tsx             # ② 영역 지정
│   │   ├── AxisPanel.tsx               # ③ 축 확인·수정
│   │   ├── SeriesPanel.tsx             # ④ 계열 선택
│   │   ├── ExtractPanel.tsx            # ⑤ 추출 + 추가 질의
│   │   ├── ResultTable.tsx             # 아래 결과 표
│   │   └── ErrorNotice.tsx             # 3단계 실패 안내 띠
│   ├── lib/
│   │   ├── types.ts                    # 5.1의 타입 정의
│   │   ├── errors.ts                   # 7장 오류 코드·문구 표
│   │   ├── pdf.ts                      # PDF → 그림 변환, 페이지 수 세기
│   │   ├── crop.ts                     # 영역 잘라내기·축소·압축
│   │   ├── axis.ts                     # 5.3 변환식 (선형/로그)
│   │   ├── trace.ts                    # 5.5 계열 색상 픽셀 추적 (브라우저 전용)
│   │   ├── plotBox.ts                  # 5.6 축 선 감지로 plotBox 보정 (브라우저 전용)
│   │   ├── csv.ts                      # CSV 만들기
│   │   ├── schema.ts                   # Zod 응답 형식 정의
│   │   └── vision.ts                   # 서버 전용 — 모델 호출 (여기서만 API 키 사용)
│   └── store/
│       └── session.ts                  # 5.2 작업 상태 (Zustand)
├── .env                                # OPENAI_API_KEY (커밋 금지)
├── PRD.md / prd_lite.md / PLAN.md / DESIGN.md / CLAUDE.md
└── trash-can/                          # 삭제 대신 옮겨두는 곳
```

---

## 10. 보안 점검

- [ ] `OPENAI_API_KEY`는 `.env`에만, `NEXT_PUBLIC_` 접두사 금지
- [ ] 모델 호출은 `src/lib/vision.ts` + `app/api/*/route.ts`에서만 (브라우저 코드에서 import 금지)
- [ ] 브라우저 네트워크 탭에 외부 모델 도메인 요청이 하나도 없어야 함
- [ ] 서버에 파일 쓰기·DB 저장 코드가 없어야 함
- [ ] 응답에 원본 에러 메시지·스택 트레이스가 담기지 않아야 함
- [ ] 전송되는 조각은 서버에서도 검사한다 — base64 길이 상한(약 4MB), MIME이 `image/jpeg`인지, 가로·세로 최소 100px 이상인지. 원본 파일의 크기·페이지 수 검사는 브라우저 전용이며, 원본 자체가 서버로 전송되지 않으므로 이 검사를 우회해도 서버가 받는 것은 작은 조각뿐이다.
- [ ] 업로드 화면에 "그래프 영역이 외부 AI 모델로 전송된다"는 사실과 "공개 자료만 사용" 전제를 안내하는 문구가 있다
- [ ] `.env`, `node_modules/`, `trash-can/`이 `.gitignore`에 등록되어 있을 것

---

## 11. 테스트 계획

### L1 — API 테스트

| # | 대상 | 확인 내용 | 기대 |
|---|---|---|---|
| 1 | `/api/analyze` | 정상 차트 조각 | 200, `plotBox`·`series` 존재 |
| 2 | `/api/analyze` | 차트가 아닌 이미지 | `REGION_NO_CHART` |
| 3 | `/api/analyze` | 이미지 필드 없음 | 400 |
| 4 | 모든 경로 | 응답 본문 | 원본 에러 메시지가 없을 것 |

### L1.5 — 픽셀 추적 테스트 (§5.5, 브라우저 계산이라 API 호출 없음)

| # | 대상 | 확인 내용 | 기대 |
|---|---|---|---|
| 1 | `trace.ts` | 정답을 아는 그래프 | 오차 ±3% 이내, 50/50 지점 |
| 2 | `trace.ts` | 같은 이미지 3회 반복 | 값이 완전히 동일(계산이므로) |
| 3 | `trace.ts` | 계열 색이 없는 세로줄 | `low` + "확인 필요(선 못 찾음)" |
| 4 | `trace.ts` | 선이 겹치는 구간 | `medium` 또는 "확인 필요(교차)" |

### L2 — 화면 동작 테스트

| # | 화면 | 동작 | 기대 |
|---|---|---|---|
| 1 | 업로드 | 25MB 파일 선택 | `FILE_SIZE` 문구, 판독 시도 안 함 |
| 2 | 축 확인 | 유형 미선택 상태 | 다음 단계 버튼 비활성 |
| 3 | 축 확인 | 로그축에 최솟값 0 입력 | 경고 표시 |
| 4 | 계열 선택 | 두 개 선택 시도 | 하나만 선택됨 |
| 5 | 결과 표 | low 신뢰도 지점 | "확인 필요" 배지 |
| 6 | 전체 | "합격/불합격" 문구 검색 | 0건 |

### L3 — 전체 흐름 테스트

| # | 시나리오 | 성공 기준 |
|---|---|---|
| 1 | JPG 업로드 → 영역 → 축 확인 → 계열 → 추출 → CSV | 끊김 없이 완주, CSV 열림 |
| 2 | PDF 5페이지 중 3페이지 그래프 | 페이지 이동 후 정상 추출 |
| 3 | 검증용 그래프 3회 반복 추출 | 값 일치율 95% 이상 |
| 4 | 새로고침 | 모든 작업 내용 사라짐 |

### 검증용 데이터

정답을 **수식으로** 아는 그래프 8개를 `public/test-charts/`에 두었습니다(브라우저에서
`/test-charts/`로 열면 그려지고 JPG로 내려받을 수 있습니다). 각각 다른 위험 요소를 하나씩 담았습니다 —
기본형·로그축·점선+교차·3계열·흑백점선·마커·격자테두리·축에 붙는 급한 곡선.

**측정 결과는 [VERIFICATION.md](VERIFICATION.md)에 있습니다.** 요약: 8개 중 6개 그래프(9개 계열)가
평균 오차 0.3% 이하로 목표(±3%)를 통과했고, 재현성은 100%입니다.

### 알려진 한계 (실측으로 확인)

| 상황 | 결과 | 성격 |
|---|---|---|
| **흑백 점선 차트** | 평균 오차 10~24% | **구조적 한계.** 이 앱은 계열 색으로 선을 구분하는데(§5.5) 무채색은 갈라낼 수 없다. 임계값 조정으로 해결 불가 |
| **곡선이 축 선에 바짝 붙는 구간** | 평균 오차 27% | 축 선을 데이터로 오인. plotBox 가장자리 제외를 시도했으나 다른 그래프를 악화시켜 되돌림 — 미해결 |
| **AI 판독의 실행 간 편차** | 같은 그래프도 `분석`을 다시 누르면 결과가 달라질 수 있음 | 픽셀 추적 자체는 100% 재현되지만, 앞단 AI가 주는 색상·plotBox가 매번 조금씩 다름 |

---

## 12. 구현 순서 (PLAN.md 매핑)

| 묶음 | PLAN 단계 | 이 설계서에서 볼 곳 |
|---|---|---|
| 준비 | 1~5 | §5.1 타입, §5.2 상태, §7 오류 표, §8 기술 |
| 이미지 올리기 | 6~8 | §3.2 ①, §8.2 pdf.js |
| 영역 지정·서버 왕복 | 9~11 | §3.2 ②, §4.1, §8.3 크기 제한 |
| 판독·축 확정 | 12~17 | §6.1, §3.2 ③, §7 영역·모델 단계 |
| 좌표 추출 + CSV | 18~26 | §6.2, §5.3 변환식, §3.3 결과 표, §8.2 CSV |
| 마무리·검증 | 27~31 | §10 보안, §11 테스트 |

### 세션 나누기 제안

| 세션 | 범위 | PLAN 단계 |
|---|---|---|
| 1 | 뼈대 + 업로드 + 이미지 표시 | 1~8 |
| 2 | 영역 지정 + 서버 왕복 + 판독/축 확정 | 9~17 |
| 3 | 좌표 추출 + 결과 표 + CSV | 18~26 |
| 4 | 마무리 검증 | 27~31 |

---

## Version History

| 버전 | 날짜 | 변경 내용 |
|---|---|---|
| 0.1 | 2026-08-13 | 최초 작성 (설계안 B 선택 반영) |
| 0.2 | 2026-08-13 | design-validator 검토 반영 — Critical 5건(축 정보 전송 여부 통일, tx를 앱이 고정, plotBox·ty 좌표 원점 정의, 역변환식 추가, 계열 교차 판정 구조 수정) + Important 10건(기준 미지정 구간 처리, 구간 매칭 규칙, 서버 업로드 검사 범위, cropRect 보관, CSV 근거 헤더, 공개 자료 안내, 오류 HTTP 상태 코드, 축 범위 밖 지점 표시 방식 등) |
| 0.3 | 2026-08-13 | **범위 축소** — 기준값 비교·검토 기능(구간별 상한·하한, 이탈 강조, §5.4)을 다음 단계로 보류. 좌표 추출 결과를 CSV로 내려받는 것까지가 이번 버전 범위. `Threshold`·`ComparisonRow` 타입, `ThresholdPanel`·`compare.ts` 제거, 결과 표에서 기준·차이·판정 열 제거 |
| 0.4 | 2026-08-13 | **판독 방식 하이브리드 전환 (B안 → D안)** — 정답을 아는 검증용 그래프 실측 결과 AI 좌표 판독이 평균 오차 5.9~7.7%(최대 19.8%)·실행마다 값 변동으로 PRD 목표(±3%, 95%) 미달. 앱이 계열 색상으로 픽셀을 추적하는 방식으로 바꿈. §2 설계안 비교 갱신, §5.5 픽셀 추적 규칙 신설, `/api/extract`·`/api/point` 제거(외부 모델 호출은 `/api/analyze` 1회로 축소), `trace.ts` 추가 |
| 0.6 | 2026-08-13 | **§5.5 알고리즘 보완 + 검증 (PLAN 28~30)** — 연속성 추적·점선 넓히기·색 팔레트 대조를 §5.5에 문서화. 검증용 그래프 8개(`public/test-charts/`)를 만들어 실측: 6개 그래프·9개 계열이 평균 0.3% 이하로 ±3% 통과, 재현성 100%. 흑백 점선·축에 붙는 곡선은 미해결 한계로 §11에 기록. 결과는 [VERIFICATION.md](VERIFICATION.md) |
| 0.5 | 2026-08-13 | **plotBox 픽셀 보정 추가 (§5.6)** — 하이브리드 전환 후에도 앱 실측 오차가 4.08%로 남아 원인을 추적한 결과, AI가 `plotBox`를 `{0.1,0.1,0.9,0.9}`로 반올림해 답하면서 아래쪽 경계가 7.9% 어긋난 것이 원인이었다. 축 선을 픽셀에서 찾아 보정하도록 `plotBox.ts` 추가. **최종 실측: 평균 0.083%·최대 0.222%·50/50이 ±1% 이내·재현성 100%(3회 반복 전부 동일)** |
