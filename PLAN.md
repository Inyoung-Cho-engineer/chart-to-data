# PLAN.md — chart-to-data 1차 구현 계획

> 근거 문서: [PRD.md](PRD.md) / 규칙: [CLAUDE.md](CLAUDE.md)
> 작성 기준일: 2026-08-13

---

## 이번 사이클 목표

PRD에 정의된 **핵심 기능**(그래프 좌표 추출)을 Next.js 단일 프로젝트로 처음부터 끝까지 동작하게 만든다.
PDF/JPG를 올려 → 영역을 드래그로 지정하고 → 축 정보를 확인·보정한 뒤 → 계열 1개를 골라 좌표 50개 + 신뢰도를 뽑고 → CSV로 내려받는 흐름 하나를 완성하는 것이 이번 사이클의 전부다.

### 이번에 만들지 않는 것
PRD 6장 비범위 그대로. **기준값 비교·검토 기능(구간별 상한·하한, 이탈 지점 강조)은 다음 단계로 보류한다** — 이번 버전은 추출값을 정확히 뽑아 CSV로 내려받는 것까지가 범위다. 그 외 라인차트 외 그래프 유형, 여러 계열 일괄 추출, 로그인·협업, 다국어, 모바일, 대량 일괄 처리, 별도 Python 추출 서버.

---

## 성공 기준

| # | 기준 | 확인 방법 |
|---|---|---|
| S1 | 업로드 → 영역 지정 → 축 확인 → 계열 선택 → 추출 → CSV까지 한 번에 끊김 없이 진행된다 | 브라우저에서 전체 흐름 1회 완주 |
| S2 | 추출 좌표 오차율이 축 범위 대비 ±3% 이내 | 정답값을 아는 검증용 그래프 5~10개로 측정 |
| S3 | 같은 그래프를 여러 번 추출했을 때 값 일치율 95% 이상 | 동일 그래프 3회 반복 추출 후 비교 |
| S4 | 그래프 1개당 추출 소요 3분 이내 | 실제 조작 시간 측정 |
| S5 | 이미지가 브라우저에서 외부로 직접 나가지 않는다 (모델 호출은 서버 라우트에서만) | 네트워크 탭에 외부 모델 도메인 요청이 없음을 확인 |
| S6 | 실패 3단계(파일/영역/모델 응답)가 각기 다른 안내 문구로 뜨고, 원본 에러 메시지가 노출되지 않는다 | 3가지 실패를 일부러 발생시켜 확인 |
| S7 | 새로고침하면 작업 내용이 사라진다 (서버·DB에 아무것도 남지 않음) | 새로고침 후 상태 초기화 확인 |

---

## 작업 순서

> 규칙: 위에서부터 순서대로, 한 번에 한 개씩. 각 작업을 마칠 때마다 브라우저나 실행으로 **눈으로 확인**한 뒤 다음으로 넘어간다.

### 0단계 — 바닥 다지기 (먼저 해두면 이후가 전부 쉬워지는 것)

1. Next.js 프로젝트를 `my-app`에 생성하고 빈 페이지 하나가 뜨는 것까지 확인한다.
2. `.env`의 OpenAI 키를 **서버 쪽에서만** 읽는 설정을 넣고, 키가 없을 때 조용히 죽지 않고 안내되도록 한다.
3. 앱 전체가 쓸 **타입/데이터 구조**를 한 파일에 먼저 정의한다 (축 정보, 계열, 추출 지점, 신뢰도, 기준 구간, 비교 결과).
4. 추출 상태 전체를 담는 **브라우저 세션 상태 저장소**를 만든다 (서버 저장 없음, 새로고침 시 소멸).
5. 실패 3단계(파일/영역/모델 응답)의 **에러 코드와 안내 문구를 한 곳에 표로 먼저 정의**하고, 화면에 띄우는 공용 안내 컴포넌트를 만든다.

### 1단계 — 이미지를 화면에 올리기

6. PDF/JPG 업로드 화면을 만든다.
7. 업로드 검사(형식·20MB 이하·PDF 30페이지 이하)를 넣고, 초과 시 판독을 시도하지 않고 5번의 파일 단계 문구를 띄운다.
8. 업로드한 이미지를 화면에 표시한다 (PDF는 이미지로 변환, 여러 페이지면 페이지 선택).

### 2단계 — 영역 지정과 서버 왕복

9. 이미지 위에서 드래그로 사각 영역을 지정하는 기능을 만든다 (자동 탐지 없음).
10. 지정한 영역만 잘라낸 이미지를 만들어 미리보기로 보여준다.
11. 잘라낸 이미지를 받는 서버 API 경로를 만든다 (아직 판독 없이 잘 받았는지만 확인).

### 3단계 — 모델 판독과 축 확정

12. 서버에서 외부 비전 모델을 호출해 응답을 받아온다 (응답을 그대로 화면에 출력해 확인).
13. 모델에 요구할 **응답 JSON 형식을 고정**하고, 형식이 어긋나면 5번의 모델 응답 단계 문구로 처리한다.
14. 응답에서 축 유형(선형/로그)·범위·단위를 뽑아낸다.
15. 뽑아낸 축 정보를 화면에 보여주고 사용자가 직접 고칠 수 있는 입력칸을 만든다 (확인 전에는 추출로 넘어가지 못하게 막는다).
16. 축 유형을 판별하지 못했으면 임의로 선형이라 가정하지 말고 사용자가 직접 고르게 한다.
17. 지정 영역에서 라인차트를 찾지 못한 경우 5번의 영역 단계 문구를 띄운다.

### 4단계 — 좌표 추출

18. 인식된 계열(선) 목록을 화면에 보여준다.
19. 계열을 **한 번에 1개만** 선택하는 기능을 만든다.
20. 선택한 계열에서, 앱이 먼저 X축 균등 50지점의 위치(tx)를 계산해 함께 보내고 그 각각의 이미지상 세로 위치(ty)와 신뢰도(high/medium/low)를 요청해 받아온 뒤, 앱이 변환식으로 실제 좌표값을 계산한다 (DESIGN.md §5.3 — AI는 위치만, 계산은 앱이).
21. 축 범위를 벗어난 값을 걸러내는 검사를 넣는다.
22. 추출된 좌표값을 표로 보여준다.
23. 신뢰도가 low인 지점과 계열이 교차하는 구간을 "확인 필요"로 표시한다.
24. 원본 이미지와 추출 결과 표를 나란히 배치한다.
25. 사용자가 특정 X값을 입력해 그 지점의 값을 추가로 질의하는 기능을 만든다.
26. 추출값·신뢰도를 CSV로 내려받는 기능을 만든다.

### 5단계 — 마무리와 검증

27. 실패 3단계 문구가 각각 제대로 뜨는지 일부러 실패를 만들어 확인하고, 원본 에러 메시지가 새어 나가지 않는지 점검한다.
28. 정답값을 아는 검증용 그래프 5~10개를 준비한다.
29. 검증용 그래프로 오차율(±3%)·재추출 일치율(95%)을 측정해 목표 대비 확인한다 (S2·S3).
30. 전체 흐름을 처음부터 끝까지 한 번 완주하며 다듬는다 (S1·S4·S5·S7 확인).
31. Vercel 배포 + 환경변수 등록으로 마무리한다.

---

## 위험과 대응

| 위험 | 대응 |
|---|---|
| 비전 모델이 좌표를 부정확하게 읽어 ±3%를 못 맞춤 | 축 정보를 사용자 확인·보정 후 넘기고, 픽셀 좌표 → 실좌표 변환은 앱에서 직접 계산해 모델 의존도를 낮춘다 (29번에서 측정 후 판단) |
| 같은 그래프에서 값이 매번 달라져 일치율 95% 미달 | 온도 0·응답 형식 고정, 29번에서 반복 측정해 확인 |
| 모델 응답 형식이 들쭉날쭉함 | 13번에서 JSON 스키마를 먼저 못 박고 검증 실패 시 모델 응답 단계 실패로 처리 |
| 로그 스케일 판별 실패 | 16번 — 자동 판별 실패 시 사용자 지정 (임의 가정 금지) |
| PDF 변환이 무겁거나 느림 | 7번 제한(20MB·30페이지)으로 상한을 두고, 페이지 단위로만 변환 |

---

## 진행 규칙

- 한 번에 한 작업. 끝날 때마다 **무엇을, 왜 바꿨는지 한 줄**로 보고한다.
- 확인하지 않은 변경은 "완료"라고 말하지 않는다.
- 파일은 지우지 않고 `trash-can/`으로 옮긴다.
- 모든 주석·설명은 한국어로 쓴다.
